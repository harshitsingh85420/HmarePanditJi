/**
 * Subpath entry: `import { cityKey, cityForms } from "@hmarepanditji/utils/city-vocab"`.
 * Same reason as ./code-only.ts: guards and the API need the vocabulary
 * without pulling the barrel.
 */
export * from "./src/city-vocab";
