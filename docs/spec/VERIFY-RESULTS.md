# VERIFY results — the 25 🔍 items, resolved against code

Every item the register left as 🔍 was investigated in the codebase, then
every positive verdict was handed to a second reviewer whose job was to
**refute** it. That second pass downgraded 4 claims, which is the reason
it exists: a false ✅ means Isj ships believing a promise is kept.

**Outcome: built 1 · partial 7 · missing 17** (of 25)

Register tally after applying these: ✅ 8 · 🟡 14 · ❌ 56 · 🔍 0 (78 total).

---

## F12-03 — ✅ built

**What the code does today.** The supply-preference question is genuinely implemented as behaviour (not just copy) in TWO places, at two different scopes. (1) Readiness step R2 asks it ONCE GLOBALLY as a true हाँ/नहीं binary: the question is rendered, spoken as the step narration, answerable by tap (YesNoRow) or voice, cannot be skipped (null blocks advance), and both branches produce distinct behaviour — नहीं shows the "सामान ग्राहक/प्लेटफ़ॉर्म का रहेगा" card and waives package pricing; हाँ reveals the per-pooja package builder and requires ≥1 priced tier per pooja. It persists to the global PanditProfile.canBringSamagri column. (2) The per-pooja add-a-pooja wizard (my-poojas/add step 2) asks a SEPARATE supply question "सामान कौन लाएगा?" immediately after the tier item list, but as a THREE-way choice (PANDIT_BRINGS / PLATFORM_SELLS / LIST_ONLY) persisted per-pooja to PoojaConfig.supplyMode — not a हाँ/नहीं pair. The SamagriTiers component asks nothing; it only reads a showPrices flag. Two divergences from the doc worth recording, neither of which defeats the requirement: (a) the literal doc string "क्या आप ये सारी सामग्री खुद ला सकते हैं?" appears nowhere in the codebase — the shipped copy is "क्या आप पूजा का सामान खुद ला सकते हैं?" (strings.ts:334); the doc's "ये सारी" ("all THESE items") implies the question follows a displayed item list, i.e. per-pooja, whereas the हाँ/नहीं implementation is the global readiness one. (b) The two answers are not reconciled anywhere: nothing gates or prefills the per-pooja supplyMode from the global canBringSamagri, so a pandit who answered नहीं globally can still pick "हाँ, मैं लाऊँगा" for an individual pooja. That contradiction is a real product gap but belongs to a consistency requirement, not to F12-03's "is it asked with हाँ/नहीं branches".

**Evidence.**
- apps/pandit/src/lib/strings.ts:334 — `r2Question: "क्या आप पूजा का सामान खुद ला सकते हैं?"` (the actual asked copy; note it is NOT the doc's literal "ये सारी सामग्री" wording)
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:909-921 — StepR2 renders the question as an <h2> plus `<YesNoRow value={canBring} onChange={setCanBring} />`, i.e. a real binary हाँ/नहीं control, not just copy
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:914-920 — the नहीं branch renders a distinct outcome card `r2NoInfo` ("कोई बात नहीं — सामान ग्राहक/प्लेटफ़ॉर्म का रहेगा", strings.ts:335)
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:923-946 — the हाँ branch renders the per-pooja package-builder list (one card per specialization, ✓ पूरा हुआ / कीमत भरना बाकी), opening SamagriPackageEditor
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:420-434 — saveR2: unanswered (`canBring === null`) blocks advance and re-speaks the question; on हाँ it additionally requires ≥1 priced tier for every pooja; persists via `patchStep(2, { canBringSamagri: canBring })`
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:283-315 — voice grammar: on step 2 the pure YES keywords set canBring=true and NO sets canBring=false (instead of advancing), so the question is answerable by voice
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:594 — the step-2 screen narration IS `r2Question`, so it is spoken, not merely displayed
- services/api/src/controllers/readiness.controller.ts:133-152 — server validates `canBringSamagri` is boolean, requires priced packages when true, writes `update.canBringSamagri`

**Test that would pin it.** Component test on the readiness wizard: render /readiness forced to step 2 with a mocked /pandit/readiness snapshot (canBringSamagri: null, specializations: ['SATYANARAYAN'], samagriTiersByPuja: {}). Assert (i) the text of strings.readiness.r2Question is in the DOM; (ii) pressing आगे with no answer does NOT call patchStep and surfaces the question as the error; (iii) clicking नहीं renders the r2NoInfo card and pressing आगे PATCHes { canBringSamagri: false } and moves to step 3 without requiring any package price; (iv) clicking हाँ renders one card per specialization with the कीमत भरना बाकी badge and pressing आगे is BLOCKED with r2Error until samagriTiersByPuja[spec] > 0. Separately, drive the step-2 voice registry with a pure YES and a pure NO utterance and assert canBring flips rather than the step advancing. For the per-pooja half, extend the existing apps/pandit/src/app/(dashboard-group)/my-poojas/add/stepModel.test.ts to assert the supply group offers exactly three options and that only PANDIT_BRINGS sends non-null tier prices to /pandit/samagri-packages.

## F02-02 — 🟡 partial

**What the code does today.** A failure-1 apology does fire, but only inside VoiceField, and the string is not the doc's. VoiceField's machine speaks 'माफ़ कीजिए, समझ नहीं आया — फिर बोलें या नीचे लिख दें' on the first unparseable/low-confidence transcript. The doc's exact line 'माफ़ कीजिये, कृपया फिर से बोलिए।' appears nowhere. The universal path — voiceController.speakUnmatchedGently, which handles every transcript not claimed by a field — is not a ladder at all: it is a 30s-per-screen throttle around a single line ('समझ नहीं आया — फिर बोलें'), with no attempt counting. A doc-shaped rung-1 string does exist in useVoiceCascade.ts, but that hook is dead code, explicitly labelled so by the repo's own guard test.

**What is missing.** The doc's wording ('माफ़ कीजिये, कृपया फिर से बोलिए।') and universality: only VoiceField apologizes on failure 1; screen-level/global voice input (voiceController.speakUnmatchedGently) uses a time throttle, not a failure counter.

**Evidence.**
- apps/pandit/src/components/voice/voiceFieldMachine.ts:50-59 — on TRANSCRIPT with `!parsed || e.confidence < 0.55`: `const n = a + 1; return { next: {phase:'LISTENING'}, attempts: n, effects: n === 1 ? ['SPEAK_SORRY_ONCE'] : ['START_LISTEN'] }` — exactly one apology, on failure #1 only
- apps/pandit/src/components/voice/VoiceField.tsx:115-121 — `case "SPEAK_SORRY_ONCE": voiceController.speak(t("voiceLoop.sorryOnce"), …)`
- apps/pandit/src/lib/strings.ts:181 — `sorryOnce: "माफ़ कीजिए, समझ नहीं आया — फिर बोलें या नीचे लिख दें"`
- apps/pandit/src/components/voice/voiceFieldMachine.ts:2-3 — header comment: "Failures NEVER stop the loop: at most ONE gentle apology per field" (the design is deliberately one rung)
- apps/pandit/src/lib/voiceController.ts:915-936 `speakUnmatchedGently` — the SHARED/global path for unclaimed speech: a 30-second per-screen time throttle (`if (now - last < 30000) { this.loopRearm(); return; }`) then `this.speak(t("voiceLoop.unmatched"))`; there is no attempt counter here at all
- apps/pandit/src/lib/strings.ts:182 — `unmatched: "समझ नहीं आया — फिर बोलें"`
- apps/pandit/src/lib/hooks/useVoiceCascade.ts:79 — `case 1: return \`माफ़ कीजिए, फिर से कहें. …\`` — the doc-shaped rung-1 line, in a hook with zero consumers
- apps/pandit/src/lib/oneVoiceOwner.test.ts:49 — the repo's own guard test calls `/lib/hooks/useVoiceCascade` "its (dead) lib wrapper", confirming it is not wired to any screen

**Test that would pin it.** Unit-test the reducer: `reduce({phase:'LISTENING'}, {type:'TRANSCRIPT', text:'zzz', confidence:0.2}, {attempts:0,…})` must return effects `['SPEAK_SORRY_ONCE']`, and a VoiceField render test must assert voiceController.speak was called with the doc's exact string. A second test should drive voiceController.handleTranscript with unmatched text and assert an apology on the first failure rather than a 30s throttle.

## F02-05 — 🟡 partial

**What the code does today.** The scope requirement is met where a ladder exists, but only there, and the reset rules are incomplete. VoiceField holds `attempts` in component-local state, so two VoiceFields on one screen have independent counters and a fresh mount starts at zero — correct per-field semantics. However it resets to 0 only on CONFIRM_YES; a CONFIRM_NO, a typed takeover or an STT_FAILED leaves the count elevated, so after one failure a long-lived field never apologizes again. And the counter governs a one-rung ladder only (see F02-02/03/04), so per-field scoping buys almost nothing. Everywhere outside VoiceField the accounting is not per-field: voiceController.speakUnmatchedGently throttles per screen on elapsed time, and the two (dead) cascade hooks count into a single global zustand store — explicitly session-scoped, the exact thing the doc forbids.

**What is missing.** Per-field counting outside VoiceField: the shared voiceController path has no per-field counter (per-screen 30s timer instead), and the cascade hooks use the app-global voiceStore.errorCount. Also missing: a reset on CONFIRM_NO / TYPED_INPUT / STT_FAILED, so the counter is sticky within a field's lifetime.

**Evidence.**
- apps/pandit/src/components/voice/VoiceField.tsx:66 `const [attempts, setAttempts] = useState(0);` and :71-72 `const attemptsRef = useRef(attempts); attemptsRef.current = attempts;` — the counter is component-local React state, so each mounted VoiceField instance owns its own count
- apps/pandit/src/components/voice/VoiceField.tsx:97-103 — `reduce(stateRef.current, e, { attempts: attemptsRef.current, … })` then `setAttempts(result.attempts)`: the field's own count is the only input to the ladder decision
- apps/pandit/src/components/voice/voiceFieldMachine.ts:70-72 — CONFIRM_YES returns `attempts: 0`: the counter resets when the field's value is accepted
- apps/pandit/src/components/voice/voiceFieldMachine.ts:36-39 (TYPED_INPUT), :63-66 (STT_FAILED), :73-75 (CONFIRM_NO) — all return `attempts: a` unchanged; the count is never cleared on decline, on typing, or on an STT error
- apps/pandit/src/lib/voiceController.ts:928-933 — the shared/global unmatched path keys off `entry.lastUnmatchedAt` (per SCREEN, time-based) or `this.lastGlobalUnmatchedAt`, not a per-field counter
- apps/pandit/src/stores/voiceStore.ts:19,43 `errorCount: 0`, :59-67 `incrementError` (drives error_1/error_2/error_3 and flips isKeyboardMode at 3), :69 `resetErrors` — this counter, the one the two cascade hooks use, is a single global zustand store shared app-wide, i.e. session-scoped not field-scoped
- apps/pandit/src/lib/hooks/useVoiceCascade.ts:52-64 and apps/pandit/src/lib/hooks/useSarvamVoiceFlow.ts:151,229 — both read/write that global errorCount

**Test that would pin it.** Render two VoiceFields on one screen; drive field A to a failure, then drive field B and assert B still gets the failure-1 apology (proves independence). Then re-mount field A and assert its count restarts at 0. Finally assert the same independence via the shared voiceController path — today that path has no per-field counter to assert against, which is the gap.

## F02-06 — 🟡 partial

**What the code does today.** The confirmation loop is universal for VoiceField free-value capture (text/number/money/phone/choice) — the machine physically cannot emit a value without CONFIRM_YES, and both the spoken line and the visible row match the doc's "आपने कहा [X] — सही है?" with नहीं re-listening and हाँ advancing. It is NOT universal across all voice input: (1) every visible on-screen option selected by voice via useVoiceOptions commits instantly with no confirmation — supplyMode, teamSize, readiness specialization cards; (2) readiness step-2's spoken हाँ/नहीं sets canBring directly; (3) command-level confirmation exists only through VoiceActionListener's confirmText, used at exactly 3 call sites (booking accept/reject/complete) — controller-level confirmSpeech is an announcement, not a gate. Additionally, inside VoiceField the parsed value is written to parent state at line 128 before the pandit answers, and CONFIRM_NO never reverts it, so a mis-heard value survives an explicit "नहीं" in the parent's state even though it is not 'accepted' (no EMIT_VALUE, no onComplete).

**What is missing.** Confirmation is absent for (a) all voice option-selection (useVoiceOptions → voiceController.matchVisibleOption → onSelect, voiceController.ts:800-806), (b) readiness step-2 canBring boolean answers, (c) all registered voice commands except the 3 confirmText call sites. Also missing: a revert of the pre-written value on CONFIRM_NO (VoiceField.tsx:128 vs voiceFieldMachine.ts:73-75).

**Evidence.**
- apps/pandit/src/components/voice/voiceFieldMachine.ts:50-61 — TRANSCRIPT with a parseable value always goes to phase CONFIRMING with effect SPEAK_CONFIRM; there is no path from LISTENING straight to EMIT_VALUE
- apps/pandit/src/components/voice/voiceFieldMachine.ts:69-76 — EMIT_VALUE fires only on CONFIRM_YES; CONFIRM_NO returns to LISTENING
- apps/pandit/src/components/voice/VoiceField.tsx:122-138 — SPEAK_CONFIRM speaks t('voiceLoop.confirmAsk') with the parsed value
- apps/pandit/src/lib/strings.ts:184 — confirmAsk = "आपने कहा {value}। सही है? हाँ या नहीं बोलें।" (the doc's exact ceremony, wording differs by punctuation)
- apps/pandit/src/components/voice/VoiceField.tsx:383-403 — the visible confirm row renders literally "आपने कहा <value> — सही है?" with हाँ/नहीं buttons
- apps/pandit/src/components/voice/VoiceField.tsx:254-273 — live STT while CONFIRMING routes to CONFIRM_YES/CONFIRM_NO; anything else re-asks (confirmRepeat), never accepts
- apps/pandit/src/components/voice/VoiceField.tsx:128 — SPEAK_CONFIRM calls onChange(next.parsed) BEFORE any confirmation, writing the un-confirmed value into parent state
- apps/pandit/src/components/voice/voiceFieldMachine.ts:73-75 — CONFIRM_NO emits only START_LISTEN; no revert of the value written at VoiceField.tsx:128

**Test that would pin it.** AUTO: (1) unit-test reduce() in voiceFieldMachine.ts asserting no event sequence from LISTENING yields EMIT_VALUE without an intervening CONFIRM_YES — this already holds. (2) RTL test on VoiceField: dispatch a TRANSCRIPT via voiceController.registerFieldClaim injection, assert onChange was called with the parsed value, then fire CONFIRM_NO and assert onChange was called again restoring the prior value — this currently FAILS. (3) RTL test mounting my-poojas/add StepDetails, call voiceController.handleTranscript('प्लेटफ़ॉर्म बेचे', 1) and assert a confirm prompt is rendered before supplyMode changes — currently FAILS (state changes immediately).

## F03-02 — 🟡 partial

**What the code does today.** The crux of the item holds: GPS detection never silently sets the language. Location grant only records city/state and advances to a LANGUAGE_CONFIRM phase; the language is written solely inside runLanguageSwitch, which is reachable only from the explicit हाँ button/voice-YES or an explicit list pick. The prompt is genuinely confirmable, in the detected language, by tap or voice, with a 'दूसरी भाषा चुनें' escape. What does NOT match the doc is the announcement's content: the required form "आपके शहर [X] के हिसाब से हम [भाषा] सेट कर रहे हैं…" is never produced. The Hindi line says only 'your region' with no city name, and the other ten languages' lines do not reference location or detection at all — they just ask 'would you like <language>?'. There is no city/place field on LangConfirmStrings, so nothing interpolates X anywhere. Separately note two things: the module that implements the two-key hpj_lang_chosen / hpj_lang_detected discipline (languagePreference.ts) is dead — nothing imports it, so the safe-by-design persistence described in its header is not what runs; the live path is the onboarding store plus languageDetect.ts's own city/state maps, which do not consult languages.ts isEnabled(), so the prompt can propose a language the registry marks enabled:false (apps/pandit/src/lib/languages.ts:54-64) and only the runtime bundle fetch failure path (page.tsx:160-164) catches it.

**What is missing.** The detected CITY is never named in the language-suggestion prompt, and 10 of 11 confirmQuestion strings never state that a suggestion was derived from location at all. No [X] city interpolation exists (no field for it on LangConfirmStrings). The doc's exact announcement 'आपके शहर [X] के हिसाब से हम [भाषा] सेट कर रहे हैं…' is unimplemented; only a generic 'हमने आपके क्षेत्र की भाषा … पहचानी' (Hindi only) approximates it.

**Evidence.**
- apps/pandit/src/app/onboarding/page.tsx:342-355 — LocationPermissionScreen's onGranted(city,state) only stores the place (store.setDetectedCity) and moves the machine to phase LANGUAGE_CONFIRM; it never writes a language
- apps/pandit/src/app/onboarding/page.tsx:313 — const detectedCode = detectLanguage(detectedCity, detectedState); the detected code is computed for RENDER only, not persisted
- apps/pandit/src/app/onboarding/page.tsx:380-402 — phase LANGUAGE_CONFIRM renders <LangConfirmScreen2 code={detectedCode} …>; the language is actually applied only inside onYes -> runLanguageSwitch(detectedCode), i.e. behind a tap/voice हाँ
- apps/pandit/src/app/onboarding/page.tsx:141-165 — runLanguageSwitch is the ONLY writer of store.setLanguage/setPreferredLanguage; its two call sites are onYes (line 391) and the explicit list pick (line 416)
- apps/pandit/src/app/onboarding/page.tsx:84-93 — useVoiceScreen narrates lc.confirmQuestion and binds YES -> onYes, NO/"दूसरी"/"बदलो" -> onOther, so the prompt is answerable by voice as well as by the two buttons (lines 108-120)
- apps/pandit/src/lib/strings-langconfirm.ts:23-31 — hi.confirmQuestion = "हमने आपके क्षेत्र की भाषा हिन्दी पहचानी — इसी में चलें या बदलें?": says 'your region', contains no [X] city slot
- apps/pandit/src/lib/strings-langconfirm.ts:32-60 — mr/bn/ta/te confirmQuestion are plain "do you want to speak <lang>?" lines; they do not mention detection, the city, or the state at all
- apps/pandit/src/lib/strings-langconfirm.ts:11-21 — the LangConfirmStrings interface has no city/place field, so no component could interpolate [X] without a schema change

**Test that would pin it.** Component test on apps/pandit/src/app/onboarding/page.tsx: seed the onboarding store with detectedCity='mumbai'/detectedState='Maharashtra' and phase='LANGUAGE_CONFIRM'; assert (a) the rendered confirm screen contains the string 'mumbai'/'मुंबई' — this currently FAILS; (b) the rendered text names the suggested language; (c) no language write happened — spy on store.setLanguage/setPreferredLanguage and assert not-called before any interaction; (d) after clicking the yes button, setLanguage is called exactly once. Plus a silent-auto-set guard: render the LOCATION_PERMISSION phase, fire onGranted('mumbai','Maharashtra'), and assert store.setLanguage was never called and phase === 'LANGUAGE_CONFIRM'.

## F08-03 — 🟡 partial  ⬇ *downgraded from partial by the refute pass*

**What the code does today.** Recording instructions are genuinely delivered, in the component, on step 3 (वीडियो) of the live 5-step add flow — but only 3 of the 4 documented items, plus one undocumented extra. मंत्रोच्चार → "मंत्र साफ़ सुनाई दें" ✓; चेहरा visible → "आपका चेहरा दिखे" ✓; माहौल → "पूजा का माहौल दिखे" ✓; "अच्छी रोशनी हो" is an extra not in the documented set. They are delivered as on-screen text only.

**What is missing.** (1) आसन — no posture/seating instruction exists in any file in apps/pandit/src. (2) Voice delivery: for a voice-first 62-year-old persona the four items are visual ✅ lines only; the step's Narrate (line 289) omits them entirely, so a pandit relying on audio never hears any recording instruction.

**Why the first verdict was downgraded.** The rendering claim holds — apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:286 defines CHECK and lines 300-303 really render it as a Card ("अच्छे वीडियो के लिए") on step 3, reachable via line 149 (d.step === 3), and line 289's Narrate omits the checklist so "on-screen text only" is correct. But the investigator's conformance accounting is wrong. The documented set is THREE items, not four: docs/spec/requirements.yaml:241 and docs/pandit-pov-conformance-register.md:94 both read "Recording instructions delivered (मंत्रोच्चार, आसन, चेहरा साफ़)". माहौल is NOT part of F08-03 — it belongs to F08-04 at docs/spec/requirements.yaml:246, the admin-review parameters "(mantra, vidhi, face, माहौल, quality)". The investigator imported an admin-side parameter and scored it as a pandit-side recording instruction. Correct scoring: मंत्रोच्चार ✓ ("मंत्र साफ़ सुनाई दें"), चेहरा साफ़ ✓ ("आपका चेहरा दिखे"), आसन ✗ ENTIRELY ABSENT — grep for आसन across apps/pandit/src and docs/ returns only the two spec/register lines themselves, no component, voice script or i18n string. So the true state is 2 of 3 documented items with TWO undocumented extras ("अच्छी रोशनी हो", "पूजा का माहौल दिखे"), not "3 of 4 with one extra". This is the silently-missing-sub-part failure mode: a reader of the finding would believe आसन was covered when no posture/seating guidance is delivered anywhere in the pandit app. Verdict remains partial but the stated gap is materially understated. Separately, F08-04 should be checked for double-counting माहौल.

**Evidence.**
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:286 — const CHECK = ["मंत्र साफ़ सुनाई दें", "अच्छी रोशनी हो", "आपका चेहरा दिखे", "पूजा का माहौल दिखे"]
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:300-303 — CHECK is really rendered: a Card titled "अच्छे वीडियो के लिए" maps each item to a ✅ line (implementation, not copy)
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:289 — the Narrate for the video step says only "दो मिनट का वीडियो चाहिए … यूट्यूब लिंक यहाँ टाइप कीजिए।" — the checklist is never spoken
- grep across apps/pandit/src and apps/admin/src for मंत्र|आसन|माहौल returns only add/page.tsx:286 plus unrelated hits (strings.ts:234 "मूल मंत्र", referral page "आमंत्रित") — no other component delivers recording guidance

**Test that would pin it.** Render AddPooja5Page at step 3 and assert the rendered text contains all documented items including आसन; separately assert the Narrate prop text for that step includes the checklist items (currently fails on both counts).

## F11-01 — 🟡 partial

**What the code does today.** The multi-pandit quoting path is exactly one screen: the merged "और थोड़ी बातें" step of the add-pooja wizard, where the team-size picker (teamSize 1..5) sits directly above the total-dakshina money field, and teamSize is POSTed to /pandit/pooja-config alongside dakshinaAmount (page.tsx:112). On that screen the "the total covers all pandits" idea IS communicated — but only in WRITTEN text: a contextual label "{poojaName} ({teamSize} पंडितों सहित)" (line 274) and an explicit helper line "इसमें बाकी पंडितों की दक्षिणा भी शामिल है।" (line 276). The two strings that are actually SPOKEN on that screen do not carry it: the screen narration (line 238, spoken via Narrate/useScreenVoice) says only "…और कुल दक्षिणा कितनी।", and the VoiceField prompt (line 275, spoken by VoiceField.tsx:229) says only "इस पूजा की कुल दक्षिणा बोलिए". "कुल" (total) alone does not disambiguate total-across-pandits from total-for-this-pooja. The spoken confirm loop likewise re-reads only the bare number. Neither of the two other dakshina-entry surfaces mentions it at all, spoken or written: the my-poojas inline edit (page.tsx:244 → editPrompt) and onboarding readiness R1 (readiness/page.tsx:869), and neither of those screens exposes teamSize. The only Hindi sentence anywhere that states the rule in speakable form lives in the शिष्य FAQ corpus (strings.ts:692) and in no tutorial script — that is descriptive copy, not the quoting flow, and it fires only if the pandit asks "सहायक पंडितों का भुगतान कौन करता है?".

**What is missing.** Three concrete gaps. (1) No SPOKEN string on the multi-pandit quoting screen states that the total covers all pandits' dakshina — the requirement says "voice script", and both spoken strings (Narrate at add/page.tsx:238 and VoiceField promptText at add/page.tsx:275) omit it; only silent <span> text at line 276 carries it. (2) The wording is not conditional on teamSize > 1 — line 276 renders "इसमें बाकी पंडितों की दक्षिणा भी शामिल है।" even when teamSize === 1 (default, EMPTY draft at page.tsx line ~71), and line 274 renders "1 पंडितों सहित" (wrong grammar for singular). (3) The dakshina EDIT path (my-poojas/page.tsx:244) and the onboarding readiness R1 dakshina path (readiness/page.tsx:869) carry the statement in neither speech nor text, so a pandit who sets a 5-pandit total in the wizard and later re-quotes from the edit sheet gets no restatement.

**Evidence.**
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:266 — written span `{d.teamSize} पंडित (आप सहित)` under the "कितने पंडित चाहिए?" team picker; this is the ONLY multi-pandit quoting path in the app
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:272 — section heading literal "कुल दक्षिणा" (written)
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:274 — written span `{d.name} ({d.teamSize} पंडितों सहित)` above the money field
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:276 — written span `इसमें बाकी पंडितों की दक्षिणा भी शामिल है।` — the requirement's exact meaning, but rendered as a 13px <span>, never spoken; also rendered unconditionally, including when teamSize === 1
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:238 — `<Narrate text="अब तीन छोटी बातें — सामान कौन लाएगा, कितने पंडित चाहिए, और कुल दक्षिणा कितनी।" />` — this IS the spoken screen script; it says "कुल दक्षिणा" but does not say the total covers the other pandits
- apps/pandit/src/hooks/useScreenVoice.ts:33-43 — `Narrate` renders null and delegates to `useScreenVoice(text)`, i.e. the string at page.tsx:238 is TTS-spoken, confirming it is the screen's voice script
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:275 — `<VoiceField label="कुल दक्षिणा" promptText="इस पूजा की कुल दक्षिणा बोलिए" mode="money" ...>` — the spoken field prompt; no all-pandits wording
- apps/pandit/src/components/voice/VoiceField.tsx:229 — `voiceController.speak(promptText, ...)` on mount, proving promptText is the spoken prompt (so the spoken text for the dakshina field is exactly "इस पूजा की कुल दक्षिणा बोलिए")

**Test that would pin it.** React Testing Library test on the merged step of apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx (the existing suite apps/pandit/src/app/(dashboard-group)/my-poojas/add/stepModel.test.ts already drives the real step model, so extend that harness). Mock voiceController.speak and useScreenVoice, render the wizard to the merged step, select teamSize = 3, and assert that the CONCATENATION of all strings passed to speak (the Narrate screen text + the dakshina VoiceField promptText) matches /बाकी पंडित|सभी पंडित|पंडितों की दक्षिणा|पंडितों सहित/ — this fails today. Add a companion assertion that with teamSize === 1 that phrase is absent from both the spoken text and the DOM, pinning the conditionality gap. Mirror the same speak-text assertion against my-poojas/page.tsx's edit VoiceField once teamSize is surfaced there.

## F43-02 — 🟡 partial

**What the code does today.** Voice input resolves to a DISPLAY-STRING match, not to a universal ID. matchVisibleOption normalizes the transcript and substring-matches it against each visible option's rendered `label` (voiceController.ts:680-696), and VoiceOption has no id field at all (:75-79) — the match result is identified by label everywhere, including the debug log at :799. An ID only appears downstream, and only by accident of closure: in readiness/page.tsx:824-832 the onSelect callback happens to capture `spec.id`, so a spoken pooja card does end up storing SATYANARAYAN. Outside that one screen there is no ID: in the my-poojas/add wizard the pooja name and every samagri item name are captured by free VoiceField dictation and the raw transcript is POSTed as the entity key (add/page.tsx:161, 111-113, 190-195). Because the match target is the t()-translated label with no ID-keyed alias/synonym table, cross-language recognition does not exist either — a pooja is only speakable by the exact string currently rendered (plus any hardcoded `keywords` array).

**What is missing.** (1) VoiceOption/useVoiceOptions carry no `id` — the resolution contract returns a label-matched object, so nothing downstream can be guaranteed ID-correct; readiness is correct only because its closure captured spec.id. (2) No ID-keyed alias/synonym registry: matching uses the currently-rendered label plus optional per-call `keywords`, so the same pooja spoken in another language or a common variant name does not resolve. (3) The my-poojas/add pooja-name and samagri-item paths do no resolution at all — raw dictated text becomes the key. (4) Samagri items are never registered as voice options against any catalogue, so there is nothing for voice to resolve them to.

**Evidence.**
- apps/pandit/src/lib/voiceController.ts:75-79 — `export interface VoiceOption { label: string; keywords?: readonly string[]; onSelect: () => void }`. The registered option carries NO id field. Whatever voice resolves to, an ID is not part of the contract.
- apps/pandit/src/lib/voiceController.ts:680-696 `matchVisibleOption(raw)` — normalizes the transcript, then for each registered option normalizes `opt.label` and matches `clean.includes(label)`, or `label.includes(clean)` for clean.length>=3, or `matchWord(clean, opt.keywords)`. The matching key is THE PRINTED DISPLAY LABEL (with optional per-option keyword aliases). There is no ID lookup and no synonym/alias table keyed by an ID.
- apps/pandit/src/lib/voiceController.ts:797-803 — in handleTranscript: `const opt = this.matchVisibleOption(...); if (opt) { this.debug(`voice option: … → [${opt.label}]`); opt.onSelect(); return true; }` — the resolution result is the option object; it is logged and identified by label, then its closure is invoked.
- apps/pandit/src/hooks/useVoiceScreen.ts:94-113 `useVoiceOptions` — builds proxies exposing only `label`, `keywords`, `onSelect`; the doc comment at :92 is literally `useVoiceOptions(items.map(i => ({ label: i.title, onSelect: () => pick(i) })))`. The hook has no notion of an entity id either.
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:824-832 — `useVoiceOptions(SPEC_LIST.map((spec) => ({ label: specLabel(spec.id), onSelect: () => setSpecs(… spec.id …) })))`. Here the MATCH is on the translated label but the EFFECT carries `spec.id`, because the closure captured it. This is the only place a spoken pooja ends up as an ID.
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:111-112 `specLabel = (id) => t(...)` — since labels come from t(), the match target changes with the active language and there is no cross-language alias set: a pandit whose UI is in Marathi cannot hit the card by speaking the Hindi name, because only the currently-rendered label (plus optional hardcoded keywords) is in the registry.
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:161 + :111-113 — the pooja name is captured by `<VoiceField mode="text">` free dictation into `d.name`, and that raw transcript string is then POSTed as `pujaType` / `poojaType` / `poojaName`. Voice here resolves to a display string that BECOMES the entity key; no matching against any catalogue happens.
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:190-195 — samagri item names are likewise free voice dictation (`promptText="सामान का नाम बोलिए"`) stored as `{name, qty, brand}`; nothing resolves them to any item entity.

**Test that would pin it.** AUTO unit tests against the real voiceController (the pattern already used in apps/pandit/src/app/(dashboard-group)/my-poojas/add/stepModel.test.ts and mergedGrammar.test.tsx). (1) Contract test: register options via voiceController.registerOptions with an `id` field and assert handleTranscript surfaces that id to the caller — this fails to compile/assert today because VoiceOption has no id (voiceController.ts:75-79). (2) Cross-language test: mount readiness StepR1 with the active language switched so specLabel returns a non-Hindi string, feed the Hindi transcript "सत्यनारायण", and assert specs===["SATYANARAYAN"] — expected to fail, pinning the label-only matching. (3) Custom-pooja test: drive my-poojas/add step 0 by voice with "सत्यनारायण कथा" and assert the resulting POST body's pujaType equals the canonical ID rather than the transcript — today it equals the transcript (add/page.tsx:111-113).

## F01-01 — ❌ missing

**What the code does today.** The live public homepage (apps/web/app/page.tsx) has no dual-mode entry whatsoever. It renders an English customer-only funnel: hero CTAs `Book Now` → /search and a dead `Download App` button, and later `Get Started Now` → /search plus a dead `Contact Sales`. Neither required Devanagari string appears anywhere in apps/web/app. The only pandit-side entry on the public site is a small English text nav item "For Pandits" (Header.tsx:66) and a footer link (Footer.tsx:27), both pointing at NEXT_PUBLIC_PANDIT_APP_URL — a different host, defaulting to http://localhost:3002 and set in no committed config. That destination does behave correctly once reached: pandit-app `/` paints the splash and replaces to `/onboarding`, which is the voice-first orchestrator, so there is no intermediate screen at the far end. A screen matching the spec's shape exists at apps/pandit/src/app/(auth)/homepage/page.tsx, but it is in the wrong app, is unreachable (no navigation targets /homepage anywhere in apps/pandit/src — only a design-catalog table row), its customer card is a non-interactive div, its pandit CTA reads `Pandit Ke Roop Mein Judein 🪔` under the heading `क्या आप एक पंडित हैं?` rather than `मैं पंडित हूँ — जुड़ना चाहता हूँ`, and that CTA routes to `/login`, an intermediate auth screen, rather than into the voice flow.

**What is missing.** Everything on the required surface: the public homepage has zero pandit CTA, zero Devanagari CTA, and no two-CTA choice. Needed: both CTAs rendered in apps/web/app/page.tsx hero; the pandit CTA pointing at the pandit app root (which already forwards to the voice-first /onboarding); NEXT_PUBLIC_PANDIT_APP_URL actually configured so the link does not fall back to localhost:3002. Separately, the existing apps/pandit .../(auth)/homepage screen needs its customer card made interactive, its pandit button retargeted off /login, and a route into it — or deleting, since nothing links to it.

**Evidence.**
- apps/web/app/page.tsx:456-726 — the entire rendered body of the LIVE public homepage (`HomePage`); contains a language modal, tutorial modal, location prompt, hero, services grid, value props, muhurat widget, featured pandits, social proof, CTA section. No Devanagari CTA of any kind, no pandit-side entry point
- apps/web/app/page.tsx:584-589 — the ONLY hero CTA pair: `Book Now` → `/search`, and a `Download App` <button> with no onClick (dead control). Both English, both customer-facing
- apps/web/app/page.tsx:715-720 — the page's second CTA pair: `Get Started Now` → `/search` and `Contact Sales` <button> with no onClick. Again no pandit CTA
- apps/web/components/Header.tsx:62-67 — the only cross-app pandit entry on the public site: navLinks includes `{ href: panditAppUrl, label: "For Pandits", external: true }` — a small English text link, not a homepage CTA
- apps/web/components/Header.tsx:60 — `panditAppUrl = process.env.NEXT_PUBLIC_PANDIT_APP_URL || "http://localhost:3002"`; the link does cross to a different host. Grep across the repo (excluding node_modules/.next/dist) finds this var referenced ONLY at Header.tsx:60 and Footer.tsx:4 — it is set in no committed env/config file, so in a build without it the public site links to localhost:3002
- apps/web/components/Header.tsx:88 — desktop nav is `hidden md:flex`, so at 360px the "For Pandits" link is not visible at all; apps/web/components/Header.tsx:183-208 — it only appears after tapping the hamburger
- apps/web/components/Footer.tsx:27 — second entry point, a footer list item `For Pandits` → panditAppUrl, target=_blank
- apps/pandit/src/app/page.tsx:12-19 — pandit-app root `/` paints SunriseSplash and `router.replace("/onboarding")`

**Test that would pin it.** AUTO, three assertions. (1) Render test on apps/web/app/page.tsx at 360x740: assert getByText('मुझे पंडित चाहिए') and getByText(/मैं पंडित हूँ/) are both present and are anchors/buttons with a click target — this currently fails on the first query. (2) Config test: assert process.env.NEXT_PUBLIC_PANDIT_APP_URL is defined in the production env manifest and does not match /localhost/ — currently fails. (3) Playwright at 360x740: goto '/', click the pandit CTA, assert the resulting URL host equals the pandit app host and that the first painted screen is the splash/onboarding orchestrator (assert a splash/onboarding testid) with no /login or chooser hop in page.context() navigation history.

## F01-02 — ❌ missing

**What the code does today.** Judged on the required surface, the two CTAs do not exist, so there is no differentiation to assess. The nearest real pair on the live public homepage — `Book Now` vs `Download App` (page.tsx:584-589) — IS differentiated by more than text: solid primary fill + white text + coloured shadow versus a transparent 2px-border outline. But their box metrics are identical (h-12, min-w-[160px]) and they are not the customer/pandit pair. The actual pandit entry point on the public site, the "For Pandits" nav item, is differentiated from its customer siblings by text ALONE — Header.tsx:96 gives it byte-identical styling to the internal links at Header.tsx:104 — and at 360px it is not even visible until the hamburger is opened. The orphaned pandit-app screen at apps/pandit/src/app/(auth)/homepage/page.tsx does contain a strongly differentiated pair (indigo-tinted flat card vs white elevated card with a 4px saffron left rail, a 'Joining free' badge, and a 56px full-width gradient button), which would pass the 'more than text alone' bar — but only the pandit card is interactive, the customer card being a plain motion.div, so it is not a CTA pair, and the screen is unreachable.

**What is missing.** On the public homepage: both CTAs. There is no customer-vs-pandit visual differentiation to evaluate because there is no customer-vs-pandit choice rendered. The one shipped pandit affordance ("For Pandits") is differentiated by text only — same font-size, weight and colour token as the customer nav links — and is hidden behind the hamburger at 360px. The differentiated card pair that exists lives in the wrong app, is unrouted, and has only one interactive side.

**Evidence.**
- apps/web/app/page.tsx:456-726 — full rendered homepage body; the required CTA pair is absent, so there is nothing on this surface whose differentiation can be judged
- apps/web/app/page.tsx:584-589 — the actual pair a 360px visitor sees: `Book Now` is `h-12 min-w-[160px] rounded-lg bg-primary text-white font-bold shadow-xl shadow-primary/30`; `Download App` is `h-12 min-w-[160px] rounded-lg border-2 border-[#e6e1db] font-bold` with no fill. Identical height (48px) and identical min-width; they differ by fill vs outline and by text colour, so they are differentiated by more than text alone — but they are Book-vs-Download, not customer-vs-pandit
- apps/web/app/page.tsx:583 — the row is `flex flex-wrap gap-4`; at 360px inside `px-6` the two 160px-min buttons wrap to one per line, so both are full-attention but visually near-identical in size
- apps/web/components/Header.tsx:66 vs 63-65 — the pandit entry "For Pandits" carries EXACTLY the same className as the customer nav links (`text-sm font-semibold ... text-gray-700`, Header.tsx:96 external branch vs Header.tsx:104 internal branch). It is differentiated from the customer links by text alone — same size, same weight, same colour
- apps/web/components/Header.tsx:88,183-208 — and at 360px it is inside the collapsed hamburger menu (`hidden md:flex` desktop nav; mobile clone at 193 is `text-base font-semibold text-gray-800`, again identical styling to its neighbours)
- apps/pandit/src/app/(auth)/homepage/page.tsx:67 — customer card: `p-8 rounded-3xl bg-indigo-50/60 border border-indigo-100/50 shadow-sm`, heading `text-2xl font-bold text-indigo-900` (line 70)
- apps/pandit/src/app/(auth)/homepage/page.tsx:85 — pandit card: `p-8 rounded-3xl bg-surface-container-lowest shadow-[0px_8px_24px_rgba(144,77,0,0.08)] border-l-4 border-primary active:scale-[0.98]` — i.e. white/elevated with a 4px saffron left rail
- apps/pandit/src/app/(auth)/homepage/page.tsx:88-91 — pandit card carries an extra `Joining free` success-tinted badge the customer card lacks

**Test that would pin it.** AUTO, after F01-01 lands. Playwright at 360x740 on '/': locate both CTAs, then assert via getComputedStyle that they differ on at least two of {backgroundColor, color, fontSize, fontWeight, borderStyle} AND that their bounding-box heights differ by >=8px or their backgroundColor values are not both transparent — i.e. differentiation beyond the text node. Also assert both are visible without any menu interaction (boundingBox non-null immediately after load, no click on the hamburger). All three assertions currently fail because neither locator resolves.

## F02-01 — ❌ missing

**What the code does today.** There is no first-ever gate on the welcome at all. SunriseSplash speaks splash.tapHintVoice followed by shishya.intro on every single mount, and it is mounted by the root page, by the SPLASH phase, by the unknown-phase fallback, and by the frame-one veil overlay — so a returning pandit hears the welcome again on each cold entry. A persisted `firstEverOpen` flag is implemented in the onboarding stores but is dead: no component reads it. Separately, the wording differs from the doc: the app says "नमस्ते पंडित जी! मैं शिष्य हूँ — आपका सहायक।", never "यह ऐप पूरी तरह आपकी आवाज़ से चलेगा". The nearest matching sentence ('यह app आपकी आवाज़ से चलता है') lives only in voice-scripts.ts, a copy/script file used by the tutorial, which is not evidence of the welcome behaviour.

**What is missing.** (a) any read of firstEverOpen (or an equivalent localStorage/server flag) guarding the welcome utterances in SunriseSplash.tsx; (b) the doc's welcome wording — the promise that the app runs entirely on the pandit's voice is absent from the spoken welcome.

**Evidence.**
- apps/pandit/src/components/moments/SunriseSplash.tsx:118-241 — the splash's only narration effect; deps array is `[]` (line 240-241 `// eslint-disable-next-line react-hooks/exhaustive-deps`, `}, []);`) so it runs on EVERY mount with no gate of any kind
- apps/pandit/src/components/moments/SunriseSplash.tsx:172 — `await voiceController.speakAndWait(t("splash.tapHintVoice"))` and :177 `const { status } = await voiceController.speakAndWait(t("shishya.intro"));` — the two welcome utterances, spoken unconditionally
- apps/pandit/src/lib/strings.ts:22 — `intro: "नमस्ते पंडित जी! मैं शिष्य हूँ — आपका सहायक।"` (the actual spoken welcome)
- apps/pandit/src/lib/strings.ts:117 — `tapHintVoice: "नमस्ते पंडित जी! आगे बढ़ने के लिए स्क्रीन छू लीजिए।"`
- apps/pandit/src/app/page.tsx:17 — root route renders `<SunriseSplash …/>` on every load while forwarding to /onboarding
- apps/pandit/src/app/onboarding/page.tsx:323 (SPLASH phase), :518 (unknown-phase fallback) and :534 (the `!veilGone` frame-one veil, gated only by a 200ms timer at :213) — three unconditional mount sites
- apps/pandit/src/lib/onboarding-store.ts:77 `firstEverOpen: boolean`, :108 default `true`, :267 `const state = { ...DEFAULT_STATE, ...validated, firstEverOpen: false }` — a real localStorage-persisted first-ever flag EXISTS
- apps/pandit/src/lib/onboarding-store.ts + apps/pandit/src/lib/onboarding-cache-store.ts:20,70,315,329 + apps/pandit/src/stores/onboardingStore.ts:62,138 — every occurrence of `firstEverOpen` is a store definition/rehydration; a grep for `firstEverOpen` across all .tsx files returns ZERO hits, i.e. no component ever reads it

**Test that would pin it.** Component test: render <SunriseSplash/> twice with a mocked voiceController, with the onboarding-store localStorage key pre-seeded from the first render. Assert speakAndWait is called with t('shishya.intro') on the first render and NOT on the second. A second assertion should pin the welcome string to the doc's wording.

## F02-03 — ❌ missing

**What the code does today.** No second rung exists in any live code path. In VoiceField's machine, failure 2 (and 3, 4, …) emits only START_LISTEN — the app goes quiet and simply re-listens. The doc's line 'कृपया धीरे और साफ़ बोलिए।' is nowhere; the closest strings are (a) useVoiceCascade.ts:82 'ज़ोर से और धीरे-धीरे कहें' inside a hook with zero consumers and flagged dead by the repo's own guard test, and (b) voice-scripts.ts:481 'माफ़ कीजिए, फिर से बोलिए — थोड़ा धीरे और साफ़।' which is copy in a script file, not an implementation.

**What is missing.** An entire rung: a second-failure speak effect in voiceFieldMachine (and in the shared voiceController path), plus the string itself in strings.ts.

**Evidence.**
- apps/pandit/src/components/voice/voiceFieldMachine.ts:58 — `effects: n === 1 ? ['SPEAK_SORRY_ONCE'] : ['START_LISTEN']` — attempt 2 and every later attempt produce START_LISTEN only, i.e. silence
- apps/pandit/src/components/voice/voiceFieldMachine.ts:21-26 — the complete VFEffect union is START_LISTEN | STOP_LISTEN | SPEAK_CONFIRM | SPEAK_SORRY_ONCE | EMIT_VALUE; there is no second- or third-rung speak effect
- apps/pandit/src/lib/strings.ts:178-187 — the whole `voiceLoop` string block (listening/understanding/sorryOnce/unmatched/ack/confirmAsk/confirmRepeat/confirmSure); no 'धीरे और साफ़' line exists
- apps/pandit/src/lib/hooks/useVoiceCascade.ts:80-82 — `case 2: return \`ज़ोर से और धीरे-धीरे कहें. …\`` — a rung-2 line, in the dead hook
- apps/pandit/src/lib/voice-scripts.ts:481 — `'माफ़ कीजिए, फिर से बोलिए — थोड़ा धीरे और साफ़।'` — this is a voice-SCRIPT copy entry, not a component behaviour
- apps/pandit/src/lib/oneVoiceOwner.test.ts:49 — useVoiceCascade documented as dead

**Test that would pin it.** Reducer unit test: `reduce({phase:'LISTENING'}, {type:'TRANSCRIPT', text:'zzz', confidence:0.2}, {attempts:1,…})` should return a second-rung speak effect and the VoiceField render test should assert voiceController.speak called with 'कृपया धीरे और साफ़ बोलिए।'. Today it returns ['START_LISTEN'].

## F02-04 — ❌ missing

**What the code does today.** None of the three required parts happens on failure 3. The live machine's third failure produces only START_LISTEN: no fallback line is spoken, the keyboard is never auto-opened (VoiceField's inputRef is never focused — `focus()` does not appear in the file), and no 'सहायता चाहिए' button exists anywhere in live source. Two dead hooks do implement the keyboard half of the rung (useVoiceCascade.ts:112-122 and useSarvamVoiceFlow.ts:226-241), but neither is imported by any screen or component, and neither renders a support affordance either. The lone 'सहायता चाहिए' string in the tree is inside apps/pandit/eslint.json, a tooling artifact containing an unrelated help-footer snippet.

**What is missing.** All three: (1) the third-failure fallback utterance, (2) auto-focus/keyboard-mode switch on the field, (3) a 'सहायता चाहिए' button rendered on failure 3 and wired to the support path. The keyboard piece exists only in unreferenced hooks; the support button does not exist at all.

**Evidence.**
- apps/pandit/src/components/voice/voiceFieldMachine.ts:55-59 — failure 3 returns `{ next: {phase:'LISTENING'}, attempts: 3, effects: ['START_LISTEN'] }`: no fallback line, no keyboard effect, no support affordance
- apps/pandit/src/components/voice/VoiceField.tsx:67 `const inputRef = useRef<HTMLInputElement>(null);` and :352 `ref={inputRef}` — the only two references; `focus()` appears nowhere in the file, so the keyboard is never auto-opened
- apps/pandit/src/components/voice/VoiceField.tsx:317-403 — the full render: label, input/select, listening pill, confirm row. There is no support button and no failure-triggered UI branch at all
- apps/pandit/src/lib/voiceController.ts:928-935 — the shared unmatched path re-arms or speaks one line; it never switches input mode or surfaces a support control
- apps/pandit/src/lib/hooks/useVoiceCascade.ts:83-85 (`default: return 'कीबोर्ड से चुनें'`) and :112-122 (`if (error === 'KEYBOARD_FALLBACK' || errorCount >= 2) { storeSwitchToKeyboard(); onKeyboardFallback?.(); await speakWithSarvam({text:'कोई बात नहीं। अब बटन दबाकर चुनें।' …}) }`) — a real rung-3 keyboard switch, but only in the dead hook, and even there no support button
- apps/pandit/src/lib/hooks/useSarvamVoiceFlow.ts:226-241 — the other keyboard-fallback implementation (`if (errorCount >= MAX_ERRORS_BEFORE_KEYBOARD) { …switchToKeyboard(); speakWithSarvam({text:'कीबोर्ड का उपयोग करें।'}) }`); a repo-wide search shows this hook is referenced only by apps/pandit/src/test/voice-timing-audit.ts (an audit script), never by a screen or component
- apps/pandit/src/lib/oneVoiceOwner.test.ts:49 — guard test names useVoiceCascade the "(dead) lib wrapper"
- grep for 'सहायता चाहिए' across apps/: the only hit is apps/pandit/eslint.json (a lint-output artifact embedding an old JSX snippet, a page footer reading 'कोई सहायता चाहिए?' next to a support_agent icon) — not a failure-3 button and not live source

**Test that would pin it.** VoiceField integration test: drive three consecutive failing TRANSCRIPT events (unparseable text, confidence 0.2) and assert (a) voiceController.speak called with the fallback line, (b) document.activeElement is the field's input (or isKeyboardMode true), (c) a button with text 'सहायता चाहिए' is in the DOM and its click navigates to the support route. All three assertions fail today.

## F02-09 — ❌ missing  ⬇ *downgraded from partial by the refute pass*

**What the code does today.** The back half is largely honoured: पीछे is wired to router.back() or a pure step-decrement everywhere, the add-pooja draft is localStorage-backed so back cannot lose it, and readiness keeps all wizard answers in page state across step-back. Two real exceptions exist. (1) login onBack explicitly clears the entered OTP (setOtpValue("")). (2) readiness goBack closes the samagri editor by unmounting it, and that editor's item/price edits live in local useState persisted only on explicit save, so back silently discards them. The deletion half is essentially unimplemented: there is no "हटा दो" voice verb anywhere in the grammar or any screen registry, and the single delete affordance (my-poojas ✖) fires the DELETE request on first tap with no confirmation at all, let alone a double confirm.

**What is missing.** No "हटा दो" (or any delete) keyword exists in apps/pandit/src/lib/voiceGrammar.ts or in any useVoiceCommands/useVoiceOptions registry. No double-confirm gate on my-poojas/page.tsx:232 removePooja — not even single confirm. Back-safety violations: login/page.tsx:304 wipes the OTP; readiness/page.tsx:258-262 discards unsaved SamagriPackageEditor state.

**Why the first verdict was downgraded.** Refuted: the "partial" verdict overstates conformance, and three of the finding's evidentiary claims fail on inspection. (1) MISSED THIRD BACK-LOSES-DATA SITE: apps/pandit/src/app/onboarding/RegistrationScreen.tsx:38-39 holds `name` and `city` as plain useState with no persistence (line 38 comment: "ALWAYS empty — never prefilled (X3)"), and apps/pandit/src/app/onboarding/page.tsx:494-499 onBack calls store.setPhase("TUTORIAL"), unmounting the screen — so पीछे on the app's FIRST data-entry screen silently discards typed name/city. The finding's "the back half is largely honoured" was formed without this. (2) MISSED SECOND UNCONFIRMED DELETE AFFORDANCE: the finding asserts "the only delete affordance in the app is a ✖ button" at my-poojas/page.tsx:232 — false. apps/pandit/src/components/SamagriPackageEditor.tsx:168-174 is a second ✖ calling handleRemoveItem(idx) (defined :84-86), dropping a samagri item with no confirmation. (3) DEAD-CODE CITATION: apps/pandit/src/hooks/useBackButton.ts:24-42 was cited as "the shared back hook"; a repo-wide grep for `useBackButton` across apps/pandit/src returns only the definition file — zero importers. It governs nothing. CONFIRMED-BUT-RECLASSIFIED: the deletion clause is 0% implemented — no हटा/मिटा verb in apps/pandit/src/lib/voiceGrammar.ts or any screen registry, and the only confirmation machinery (confirmText, components/voice/VoiceActionListener.tsx:25,83-88) is SINGLE confirm, not double, and is wired only to booking accept/reject/complete (bookings/[id]/page.tsx:222; bookings/[id]/request/page.tsx:182,187), never to any deletion. my-poojas/page.tsx:231-237 → :133-150 does fire DELETE on first tap. F02-09 is a conjunction: clause 2 has literally no implementation, and clause 1 is worded universally ("never deletes entered data") with at least three live counterexamples (login/page.tsx:302-306 OTP wipe; readiness/page.tsx:258-262 unmounting SamagriPackageEditor whose edits live in useState per SamagriPackageEditor.tsx:42-51,96-131; RegistrationScreen name/city). The back-preserves-data behaviour that does exist is incidental — the add-pooja localStorage draft (my-poojas/add/page.tsx:87-97) was built for draft resume, not as an implementation of this law. Nothing in the codebase implements F02-09 as a requirement, so the verdict is missing, not partial.

**Evidence.**
- apps/pandit/src/components/voice/VoiceActionListener.tsx:91 — the global spoken back command is { id: 'go-back', keywords: BACK, action: () => router.back() }: pure navigation, no state mutation
- apps/pandit/src/lib/voiceGrammar.ts:31 — BACK = ["पीछे","वापस","back"] plus per-language variants (lines 60,70,78,86,94,102,110)
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:125 — onBack decrements d.step or routes out; it never touches the draft fields
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:87-97 — the draft is mirrored to localStorage on every change and rehydrated on mount, so back (and even leaving the route) preserves entered data
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:117 — localStorage.removeItem(DRAFT_KEY) happens only after a successful submit, not on back
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:256-265 — goBack stops speech, clears errorMsg, closes the editor or decrements step; none of the answer state (specs/dakshina/travel/stay/payment) is reset
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:317 and :328 — the spoken पीछे command is bound to that same goBack, so voice-back == tap-back
- apps/pandit/src/app/(dashboard-group)/samagri/page.tsx:56-63 — back only clears the selectedPuja navigation cursor

**Test that would pin it.** AUTO: (1) RTL test on my-poojas/add — type into step 0, call the Screen onBack, assert draft fields and localStorage[DRAFT_KEY] unchanged (should pass). (2) RTL test on login — enter OTP digits, click back, assert otp input still holds the digits (currently FAILS). (3) RTL test on readiness — open the samagri editor, add an item, speak पीछे via voiceController.handleTranscript('पीछे',1), reopen the editor, assert the item is still listed (currently FAILS). (4) A grammar unit test asserting a DELETE_PHRASES export exists and that voiceController.handleTranscript('हटा दो') requires two successive confirmations before any onSelect/DELETE fires (currently FAILS — no such verb).

## F02-11 — ❌ missing

**What the code does today.** There is no off-hours branch in the pandit app at all. The support screen shows a static "सुबह 8 – रात 10" caption and an always-live tel: link, with no clock check, no expected-callback-time copy, and no state where the call row degrades. The async WhatsApp option does exist and is permanently visible, but it is presented as one more equal row, not as the off-hours fallback — nothing changes between business hours and 3am.

**What is missing.** Everything time-conditional: (a) any evaluation of current time against support hours, (b) any expected-callback-time string shown when off-hours, (c) any re-presentation of WhatsApp as the async alternative when the line is closed. Only the always-on WhatsApp row exists.

**Evidence.**
- apps/pandit/src/app/(auth)/help/page.tsx:17-140 — the whole support screen; it renders re-watch tutorial, call, FAQ, WhatsApp and emergency rows with no time-dependent branch anywhere
- apps/pandit/src/app/(auth)/help/page.tsx:86 — the hours line is the hardcoded static string "सुबह 8 – रात 10 · मुफ़्त", rendered identically at 3am
- apps/pandit/src/app/(auth)/help/page.tsx:80 — the call row is an unconditional tel:+918934095599 link, never disabled or relabelled off-hours
- apps/pandit/src/app/(auth)/help/page.tsx:104-116 — the WhatsApp row (wa.me/918934095599 with a prefilled message) is always present, not surfaced as an off-hours alternative
- a repo-wide grep of apps/pandit/src for getHours|offHours|callback time|कॉलबैक returns only unrelated hits: bookings/[id]/page.tsx:112 (muhurat wording), home/HomeView.tsx:130 and components/moments/GreetingHeader.tsx:16 (time-of-day greetings) — no support-hours logic exists
- apps/pandit/src/app/(auth)/emergency/page.tsx:80-88 — the emergency path likewise dials unconditionally with no hours awareness
- apps/pandit/src/lib/strings.ts:575 — the support phone is a plain constant; there is no hours/SLA/callback field alongside it

**Test that would pin it.** AUTO: an RTL test on apps/pandit/src/app/(auth)/help/page.tsx with the system clock faked to 03:00 IST (vi.setSystemTime), asserting the screen renders an expected-callback-time string and marks the call row as closed, and a second run at 12:00 IST asserting it does not. Both would currently fail identically since the render is time-invariant.

## F03-06 — ❌ missing

**What the code does today.** There is no IP-based geolocation anywhere in the codebase, so there is no second signal that could disagree with GPS and no precedence rule to implement. The entry flow's location comes from navigator.geolocation plus a Nominatim reverse geocode, and the only fallback when that fails or is denied is a manual city picker (or, in the unused geocode.ts helper, a hardcoded Varanasi default). Client IP is read in five places but exclusively as a rate-limit key. Edge #2 is therefore not merely unproven — the condition it governs cannot arise in the current code. Verdict is missing rather than built-by-omission because the doc's intent (an IP guess exists as a fallback and GPS overrides it) is unmet in both halves.

**What is missing.** Entirely absent: (1) any IP-geolocation source, (2) any structure holding two candidate locations, (3) any precedence logic ranking GPS above IP. Nothing in languageDetect.ts / languages.ts / LocationPermissionScreen.tsx models a location source at all.

**Evidence.**
- apps/pandit/src/app/onboarding/screens/LocationPermissionScreen.tsx:83-122 — the only location acquisition in the entry flow: navigator.geolocation.getCurrentPosition -> Nominatim reverse geocode -> setDetected({city,state}); the error callback (lines 112-120) goes straight to onDenied() (the manual city picker), with no IP fallback and no second source to reconcile against
- apps/pandit/src/lib/geocode.ts:84-119 — the shared helper is GPS-only too (getCurrentLocation wraps getCurrentPosition; getFullLocation = coords -> reverseGeocode); its failure fallback is a hardcoded 'Varanasi / Uttar Pradesh' (lines 66-75), not an IP lookup
- apps/pandit/src/app/onboarding/page.tsx:357-368 — MANUAL_CITY is the only other city source, and it is a human typing a city (state passed as ""), not an IP guess
- apps/pandit/src/lib/languageDetect.ts:47-53 — detectLanguage(city,state) takes one city/state pair; it has no notion of two competing sources or of source precedence
- apps/pandit/src/lib/languages.ts:112-146 — languageForState(state) likewise takes a single state string; no precedence parameter
- repo-wide grep for ipapi|ipinfo|geoip|ip-api|ipstack|freegeoip|cf-ipcountry across apps/, services/, packages/ (excluding dist) returns zero hits; every x-forwarded-for hit is rate-limiting only — apps/pandit/src/middleware.ts:13-19 getClientIP feeds the in-memory rate-limit map (line 10), and apps/pandit/src/app/api/{tts,translate,stt-token,deepgram-token}/route.ts read it for the same purpose

**Test that would pin it.** There is nothing to pin today — a passing test would be vacuous. Once an IP source is added, the AUTO test is a unit test over the (not-yet-existing) resolver: given {gps:{city:'mumbai',state:'Maharashtra'}, ip:{city:'delhi',state:'Delhi'}} assert the resolved city is 'mumbai' and the suggested language 'mr'; given gps=null, ip present, assert the IP value is used; given both null assert DEFAULT_LANGUAGE 'hi'. Until then the only defensible guard is a negative one: assert LocationPermissionScreen's geolocation-error path calls onDenied() and performs no network call other than the reverse geocode.

## F04-01 — ❌ missing

**What the code does today.** There is no component-wise address interview anywhere in the pandit app. The pandit's address capture is a single-component city capture with an optional state tagalong: either GPS → reverse geocode → {city, state} (LocationPermissionScreen.tsx:99-101), or a 5-card/1-textbox city picker that supplies city and an empty state (ManualCityScreen.tsx:80-99, page.tsx:360-362). Of the six components the register demands (मकान नंबर → गली/मोहल्ला → शहर → ज़िला → राज्य → पिनकोड), exactly one (शहर) is captured reliably and one (राज्य) is captured on the GPS path only. मकान नंबर, गली/मोहल्ला, ज़िला and पिनकोड are not captured, not stored, and not present as identifiers anywhere in apps/pandit/src. The API side confirms it: onboarding step 1 accepts only homeCity/homeState and manufactures fullAddress by string-concatenating them (onboarding.controller.ts:97), and PanditProfile has no per-component address columns (schema.prisma:186-187). Because no components exist, the 'each through the F2 error ladder' half of the requirement is vacuously unmet as well — there is no per-component retry path to ladder. Verdict is missing rather than partial: a single city field is the pre-existing baseline this requirement was written to replace, not a partial delivery of it.

**What is missing.** All four uncaptured components (मकान नंबर, गली/मोहल्ला, ज़िला, पिनकोड) plus the sequencing that makes it an 'interview': there is no multi-step address flow, no per-component prompt, no per-component validation, and no per-component F2 error ladder. राज्य is captured only on the GPS-grant path and is set to "" on the manual-city path (page.tsx:362). Storage must also change — PanditProfile currently has only `location` and a synthesised `fullAddress` blob (schema.prisma:186-187); componentised columns exist only on the customer-side Address model (schema.prisma:151-163). Step1Body on the API (onboarding.controller.ts:19-29) would need the component fields added, since it cannot currently receive them.

**Evidence.**
- apps/pandit/src/app/onboarding/page.tsx:347-349 — the ONLY address commit in the whole onboarding machine: onGranted={(city, state) => store.setDetectedCity(city, state)}. Nothing else about a pandit address is ever collected.
- apps/pandit/src/app/onboarding/page.tsx:360-362 — the manual branch is even thinner: onCitySelected={(city) => store.setDetectedCity(city, "")} — state is passed as an empty string, so even the 2nd of the 6 required components is dropped on this path.
- apps/pandit/src/app/onboarding/screens/ManualCityScreen.tsx:24-33 — POPULAR_CITIES / CITY_SPOKEN: the manual screen is a 5-card city picker plus one free-text field. City is the only field.
- apps/pandit/src/app/onboarding/screens/ManualCityScreen.tsx:80-99 — the single <input> bound to cityInput, submitTyped() → onCitySelected(v). One free-text box, one component, no chaining to a next component.
- apps/pandit/src/app/onboarding/screens/LocationPermissionScreen.tsx:99-101 — reverse-geocode result is narrowed to exactly two values: const city = data.address.city || town || village || "Unknown"; const stateStr = data.address.state || "Unknown". road / house_number / postcode / state_district are all present in the Nominatim payload and all discarded.
- apps/pandit/src/lib/geocode.ts:44-60 — the shared geocode helper likewise returns only { city, state, country, latitude, longitude }; GeocodeResult (geocode.ts:6-12) has no house/street/district/pincode member, so no downstream code could pre-fill those components even if a screen existed.
- services/api/src/controllers/onboarding.controller.ts:97 — the server SYNTHESISES the address rather than receiving components: fullAddress: homeCity + ", " + homeState. Step1Body (onboarding.controller.ts:19-29) has homeCity and homeState and no houseNumber/street/landmark/district/pincode fields at all.
- packages/db/prisma/schema.prisma:186-187 — PanditProfile stores `location String` (city) and `fullAddress String?` as ONE blob column. There are no per-component columns on PanditProfile; the componentised columns (landmark at schema.prisma:160) exist only on the CUSTOMER-side Address model (schema.prisma:151-163).

**Test that would pin it.** Unit-test the onboarding state machine rather than the network: drive apps/pandit/src/app/onboarding/page.tsx through the manual-city branch with a stubbed store and assert the flow presents six distinct address prompts before advancing, asserting the store holds discrete houseNumber/street/city/district/state/pincode keys (today setDetectedCity(city, state) at page.tsx:348 is the only writer, so the assertion fails on shape). Pair it with a contract test on services/api/src/controllers/onboarding.controller.ts asserting onboardingStep1 persists a component-wise address instead of homeCity + ", " + homeState (line 97), and a schema guard asserting PanditProfile exposes the component columns. For the ladder half, simulate two consecutive unparseable transcripts on a single component and assert the ladder escalates within that component without discarding already-captured components.

## F04-05 — ❌ missing

**What the code does today.** The privacy requirement is not enforced; it is violated on three separate customer-reachable endpoints, all by the same root cause — a Prisma query that returns the whole PanditProfile model with no `select` narrowing, spread or passed straight into the response body. (1) GET /api/v1/pandits/:id is fully PUBLIC (no preHandler at pandit.routes.ts:1545) and returns `{ ...pandit }` from an unnarrowed `include` (pandit.controller.ts:250-258, 291-302). (2) GET /bookings/my and GET /bookings/customer/my return `include: { pandit: true }` per booking to the customer (booking.service.ts:290-292 → booking.routes.ts:145,162). (3) GET /bookings/:id returns `pandit: { include: { user: true } }` to the customer owner (booking.service.ts:240-256 → booking.routes.ts:172-178). fullAddress (schema.prisma:187) and latitude/longitude (schema.prisma:238-239) — the two things the requirement names explicitly — are all inside that shape. Treating this as the security review it was flagged as, the blast radius is materially worse than the register anticipated: the same unnarrowed shape also emits aadhaarEncrypted, aadhaarLastFour, the three KYC document URLs, bankAccountName/bankAccountNumber/bankIfscCode, upiId, adminNotes and rejectionReason — and on the public endpoint, to an unauthenticated caller who needs only a pandit id. Two facts partially bound the immediate impact without changing the verdict: PanditProfile.latitude/longitude are never written by any API code path (no `latitude` write exists in services/api/src), so those columns are null today and the coordinate leak is latent rather than live; and fullAddress is currently only ever the synthesised string homeCity + ", " + homeState (onboarding.controller.ts:97), which is city/state-grade data by accident, not by design. Neither is a control — the moment F04-03 lands GPS capture, or the moment F04-01 lands a real component-wise address, both fields start emitting genuinely sensitive data through unchanged code. The Aadhaar and bank-account exposure, by contrast, is live right now. Verdict is missing, not partial: there is no redaction layer anywhere (no Fastify response schemas on the leaking routes, no onSend hook, no filtering in sendSuccess/sendPaginated at response.ts:20-29,100-113) and no regression test guarding the contract. The two endpoints that ARE correctly narrowed (getPandits at pandit.controller.ts:141-159, favorites at customer.routes.ts:269-286) show the intended pattern exists in the codebase but was never applied to the three paths above. Recommend treating GET /pandits/:id as the priority fix given it is unauthenticated. Secondary finding outside this requirement's literal scope but inside its intent: getPandits leaks the pandit's phone number to every customer browsing the list (pandit.controller.ts:143).

**What is missing.** Three unnarrowed queries need explicit `select` whitelists reduced to the public field set (id, name, profilePhotoUrl, location/city, state, rating, totalReviews, experienceYears, specializations, languages, verificationStatus, isOnline, pujaServices) with fullAddress, latitude, longitude, aadhaarEncrypted, aadhaarLastFour, aadhaar*Url, videoKycUrl, bank*, upiId, adminNotes and rejectionReason excluded: (a) pandit.controller.ts:250-258 plus removal of the `...pandit` spread at line 291; (b) booking.service.ts:290-292 `pandit: true`; (c) booking.service.ts:240-247 `pandit: { include: { user: true } }`, whose nested `user: true` must also be narrowed to drop User.phone. Also absent entirely: any defence-in-depth layer (no Fastify response schema on the three routes, no onSend redaction hook, no filtering in the sendSuccess/sendPaginated helpers) and any regression test — no file under services/api/src/**/*.test.ts asserts on fullAddress or latitude. Separately, pandit.controller.ts:143 should drop `phone: true` from the public list response.

**Evidence.**
- services/api/src/controllers/pandit.controller.ts:250-258 — prisma.panditProfile.findUnique({ where: { userId: panditId, verificationStatus: "VERIFIED" }, include: { user: {...}, pujaServices: {...}, samagriPackages: {...} } }). `include` with NO `select` = every scalar column of PanditProfile is fetched.
- services/api/src/controllers/pandit.controller.ts:291-300 — const responseData = { ...pandit, id: pandit.user.id, user: {...}, reviewSummary }; then reply.code(200).send({ success: true, data: responseData }) at line 302. The spread re-emits every fetched scalar verbatim. Only `user` is narrowed; the PanditProfile body is not narrowed at all.
- services/api/src/routes/pandit.routes.ts:1545 — fastify.get("/:id", getPanditProfileById); registered under the '─── Public routes ───' banner (pandit.routes.ts:1321). No preHandler, no authenticate, no roleGuard, and no Fastify `schema.response` — so nothing strips fields on the way out. Mounted at /api/v1/pandits/:id (services/api/src/app.ts:370).
- LEAKED FIELDS on that response, by schema line: fullAddress (packages/db/prisma/schema.prisma:187) — the exact field the requirement names; latitude (schema.prisma:238) and longitude (schema.prisma:239) — the other field the requirement names; plus, well beyond the requirement: aadhaarEncrypted (schema.prisma:227), aadhaarLastFour (schema.prisma:228), aadhaarFrontUrl/aadhaarBackUrl/videoKycUrl (schema.prisma:172-174), bankAccountName/bankAccountNumber/bankIfscCode (schema.prisma:199-201), upiId (schema.prisma:204), adminNotes (schema.prisma:215) and rejectionReason (schema.prisma:216).
- services/api/src/services/booking.service.ts:290-292 — the CUSTOMER branch of listMyBookings: prisma.booking.findMany({ where: { customerId: userId, ...statusFilter }, include: { pandit: true } }). `pandit: true` = the entire PanditProfile row, same field set as above, once per booking.
- services/api/src/routes/booking.routes.ts:145-146 and 162-163 — GET /bookings/my and GET /bookings/customer/my call listMyBookings and pass the result straight through: sendPaginated(res, bookings as unknown[], ...). The `as unknown[]` cast is a type-level erasure, not a field filter.
- services/api/src/services/booking.service.ts:240-247 — getBookingById: include: { customer: true, pandit: { include: { user: true } } }. Whole PanditProfile PLUS the whole User row (including User.phone, schema.prisma:112).
- services/api/src/services/booking.service.ts:252-256 — the authorization check permits booking.customerId === requesterId, i.e. the customer is an authorised reader of that full object; services/api/src/routes/booking.routes.ts:172-178 returns it as sendSuccess(res, { booking }) on GET /bookings/:id with no response schema.

**Test that would pin it.** Write services/api/src/lib/pandit-privacy.test.ts as a contract test with a seeded pandit whose PanditProfile has every sensitive column populated with sentinel values (fullAddress: 'SENTINEL-ADDR', latitude: 28.6139, longitude: 77.209, aadhaarEncrypted, aadhaarLastFour, bankAccountNumber, upiId, adminNotes). Then app.inject() three requests and assert on the raw serialised body, not on parsed convenience objects: (1) GET /api/v1/pandits/:id with NO auth header — assert 200 and that JSON.stringify(body) contains none of the sentinels and that Object.keys(body.data) has no intersection with the forbidden-field list; (2) GET /api/v1/bookings/customer/my as the customer who booked that pandit — assert the same over every element of body.data; (3) GET /api/v1/bookings/:id as that customer — assert the same over body.data.booking.pandit, and additionally that body.data.booking.pandit.user has no `phone` key. Drive the forbidden-field list from a single exported constant so a future schema addition fails closed. Add a fourth case asserting GET /api/v1/pandits (list) omits user.phone. Finally, a static guard in the spirit of the existing services/api/src/lib/route-audit.test.ts: parse booking.service.ts and pandit.controller.ts and fail on any `pandit: true` or any panditProfile query using `include` without a sibling `select`, so the class of bug cannot reappear on a new endpoint.

## F05-07 — ❌ missing

**What the code does today.** There is no face-match anywhere in the product — automated or otherwise. No face/biometric comparison is computed in services/api/src (no vendor SDK, no scoring, no threshold), no schema field stores a match result or a review flag, and the admin verify endpoint has no code path that distinguishes 'mismatch → flag for human review' from any other outcome. The only face-match artefact in the repo is a manual admin checkbox labelled 'Face clearly visible and matches Aadhaar photo' in the admin video-KYC checklist, and that checkbox behaves as the OPPOSITE of the requirement: it is ANDed into canApprove, so leaving it unticked hard-disables the APPROVE button, and the only alternatives available to the admin are REJECT (sets verificationStatus REJECTED) or REQUEST_INFO (sets PENDING). There is no soft/flagged state. Separately, the reference image the check is meant to compare against is never captured: the pandit's सत्यापन step collects only Aadhaar front, back, number and consent; UPLOAD_KINDS has no selfie kind; selfieWithAadhaarUrl exists only as an unused TypeScript interface field; and the admin UI's selfie tile plus 'Compare w/ Selfie' button read pandit.documentUrls, a field that exists in neither the schema nor any API response, so the admin cannot even perform the manual comparison the checkbox asserts.

**What is missing.** Everything the item requires: (1) any face-match computation comparing the video-KYC/selfie frame against the Aadhaar photo; (2) a selfie or face-frame capture on the pandit side, plus an UPLOAD_KINDS entry and a schema column to store it; (3) a persisted match result and a 'flagged for human review' verification state distinct from PENDING/REJECTED; (4) the SOFT semantics — currently the manual proxy is a hard AND-gate on canApprove (apps/admin/src/app/pandits/[panditId]/page.tsx:29,305), so a face doubt blocks approval outright rather than routing to review; (5) a real backing field for the admin's documentUrls selfie tile and compare view, which today render dead.

**Evidence.**
- packages/db/prisma/schema.prisma:174-232 — PanditProfile carries verificationStatus, aadhaarVerified, videoKycCompleted, aadhaarFrontUrl/aadhaarBackUrl, profilePhotoUrl, adminNotes, rejectionReason. There is NO face-match score field, no faceMatch/needsReview/flaggedForReview boolean, and no selfie URL column at all.
- services/api/src/services/kyc.service.ts — grep for face/match/documentUrls returns zero hits; the KYC service performs no biometric comparison.
- services/api/src/controllers/admin.controller.ts:446-509 — updatePanditVerification (the PATCH /admin/pandits/:panditId/verify handler, routed at services/api/src/routes/admin.routes.ts:89) accepts only action APPROVE|REJECT|REQUEST_INFO plus reason/notes/requestedDocuments. It sets verificationStatus and notifies. It never computes, reads, or stores any face-match result, and there is no 'flagged for human review' state distinct from PENDING.
- services/api/src/controllers/readiness.controller.ts:267-283 — the R5 सत्यापन submit gate requires only aadhaarUrl (front), aadhaarBackUrl, a 12-digit aadhaarNumber and aadhaarConsent. No selfie/face capture is required or accepted.
- services/api/src/controllers/readiness.controller.ts:316-322 — the write block persists aadhaarConsentAt/DocUrl/FrontUrl/BackUrl/LastFour/Encrypted. No face image, no match verdict.
- services/api/src/lib/storage-keys.ts:34-42 — UPLOAD_KINDS is exactly [aadhaar-front, aadhaar-back, profile-photo, kyc-video, photo]; there is no selfie kind, and resolveKind (line 52-59) cannot produce one, so a 'selfie with Aadhaar' can never be uploaded.
- services/api/src/controllers/onboarding.controller.ts:65 — selfieWithAadhaarUrl is declared on the Step5Body TypeScript interface, but onboarding.controller.ts:207 destructures only { aadhaarFrontUrl, aadhaarBackUrl, videoUrl } and lines 220-221 persist only front/back. The selfie field is a dead type declaration, never read or stored — a type describing an intent, not an implementation.
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:147-150, 486-519 — the pandit R5 screen holds only aadhaarUrl, aadhaarBackUrl, aadhaarNumber, aadhaarConsent and submits exactly those. Its uploader (line 534-538) is typed kind: 'aadhaar-front' | 'aadhaar-back'. No selfie or face capture step exists on the pandit side.

**Test that would pin it.** A build-failing AUTO guard in services/api/src/lib/ (same style as aadhaar-consent.test.ts) asserting: (a) some module under services/api/src computes a face-match verdict and writes it to a PanditProfile column that exists in packages/db/prisma/schema.prisma; (b) a Prisma integration test that submits a verification whose face-match result is MISMATCH and asserts the resulting verificationStatus is a review state (e.g. FLAGGED_FOR_REVIEW / DOCUMENTS_SUBMITTED) and NOT REJECTED, and that the pandit's isBookingReady/booking eligibility is unchanged — i.e. the mismatch flags without blocking; (c) a static assertion over apps/admin/src/app/pandits/[panditId]/page.tsx that the 'face' checklist key is NOT a term in the canApprove expression, so a face doubt cannot hard-disable APPROVE. All three fail today.

## F08-01 — ❌ missing

**What the code does today.** There is no enumerate-and-confirm loop anywhere. The only common-pooja catalog is the 8-item SPEC_LIST in readiness R1, rendered as a simultaneous tap grid. Voice input exists but as label-matching toggles (say the pooja's name to flip that tile), not as a system-driven sequence: the app never speaks pooja names one by one and never collects a हाँ/नहीं per pooja. The live /my-poojas/add path has no catalog at all — the pandit dictates a free-text name. Grep note: pooja names appear in strings.ts:297 (i18n labels) and my-poojas/page.tsx:32 (emoji regex decoration) — copy/decoration, not an enumeration mechanism.

**What is missing.** The whole loop: (a) TTS that names each common pooja in turn, (b) a per-pooja हाँ/नहीं capture, (c) any cursor/index state advancing through the catalog. None of these exist in any component. Also: /my-poojas/add — the canon 5-step live path — exposes no pooja catalog whatsoever.

**Evidence.**
- apps/pandit/src/app/(dashboard-group)/my-poojas/add/page.tsx:156-168 — Step 0 of the LIVE 5-step पूजा जोड़ें is a free-text VoiceField ("पूजा का नाम बोलिए"), no catalog of common poojas at all
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:43-52 — SPEC_LIST, the only common-pooja catalog (8 hard-coded ids)
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:833-857 — R1 renders SPEC_LIST as a 2-column grid of tap tiles; onClick toggles membership in `specs`. All 8 render simultaneously
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:822-832 — useVoiceOptions registers each label so speaking "सत्यनारायण" toggles that tile (voiceController.ts:689 partial contains-match)
- apps/pandit/src/lib/strings.ts:330 — readiness.r1Voice is ONE open question ("आप कौन-कौन सी पूजा करवाते हैं?"); it does not name any pooja
- apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:592-598 — stepVoices: R1 gets exactly one narration; only R2 is described in-code as a हाँ/नहीं question
- apps/pandit/src/app/(dashboard-group)/my-poojas/page.tsx:27 — ALL_POOJAS is derived from hi.onboarding.specializations but is never referenced anywhere else in that file (dead)

**Test that would pin it.** Component test mounting readiness StepR1 and asserting the narration string contains no member of SPEC_LIST's labels and that all 8 tiles are in the DOM at once (i.e. no one-at-a-time cursor); plus a test on /my-poojas/add step 0 asserting no catalog options are registered with useVoiceOptions.

## F08-04 — ❌ missing

**What the code does today.** Not 5 structured fields — not even 1. Review is a binary approve/reject with a single optional free-text rejection reason, and there is no admin screen for pooja video review at all: the /admin/pooja-verifications endpoints exist server-side but have zero callers in apps/admin. apps/admin/src/app/verifications is a different queue (pandit KYC). Nothing anywhere captures mantra / vidhi / face / माहौल as separate judgements. The pandit-side checklist at my-poojas/add/page.tsx:286 is guidance copy shown to the pandit; it has no counterpart in the reviewer's data model.

**What is missing.** All 5 structured review parameters (mantra, vidhi, face, माहौल, +1) — no request fields on approve/reject, no schema columns, no UI controls. Additionally the entire admin-side pooja-verification screen is absent, so today an admin cannot approve or reject a pooja video through the product at all (only by hitting the API directly).

**Evidence.**
- services/api/src/controllers/poojaVerification.controller.ts:137-154 — approvePoojaVerification reads only the :id param; it writes status/reviewedById/reviewedAt and clears rejectionReason. No review payload is accepted at all
- services/api/src/controllers/poojaVerification.controller.ts:156-177 — rejectPoojaVerification's entire body is `{ reason }`, a single free-text string, required non-empty, stored verbatim in rejectionReason
- services/api/src/routes/admin.routes.ts:49-51 — the only admin endpoints: GET /pooja-verifications, PATCH /:id/approve, PATCH /:id/reject
- packages/db/prisma/schema.prisma:351-374 — model PoojaVerification has exactly one review-outcome field, rejectionReason String? @db.Text, next to status/reviewedById/reviewedAt. No per-parameter columns, no JSON review blob
- apps/admin/src/app/verifications/page.tsx:50 — the screen named "verifications" fetches /admin/pandits?status=PENDING, i.e. PANDIT PROFILE (Aadhaar/bank/UPI) verification, not pooja video review
- apps/admin/src/app/verifications/page.tsx:26-30, 71-124 — its review model is 3 canned rejection reasons + one free-text textarea, and approve is a bare confirm() → POST /admin/pandits/:id/approve
- grep -rln "pooja-verification" over apps/admin/src returns nothing — no admin UI anywhere calls the pooja-verification endpoints

**Test that would pin it.** API test: POST/PATCH /admin/pooja-verifications/:id/approve with a body carrying the 5 parameters and assert they are persisted and readable back via GET (currently the body is ignored and the columns do not exist). UI test: assert a route in apps/admin renders the pooja-verification queue and exposes 5 per-parameter controls.

## F09-04 — ❌ missing  ⬇ *downgraded from partial by the refute pass*

**What the code does today.** Storage retains rejection history correctly for pooja verifications, but the admin UI surfaces none of it. PoojaVerification is a genuine append-only history table (schema.prisma:351-373): every re-submit INSERTs a new row at version+1 (controller:66-81) and a rejection stamps the reason onto that specific version row (controller:163-166), so rejection count, every past reason, and which version was finally APPROVED are all recoverable from the data. The admin-facing half does not exist: no file in apps/admin references pooja-verifications at all (repo-wide grep hits only the route definitions, the pandit-side app.ts:258, and the pandit app's my-poojas page). The admin screen actually labelled 'Verification Requests' (apps/admin/src/app/verifications/page.tsx:50) is a different feature entirely — it fetches /admin/pandits?status=PENDING (profile-level KYC), shows only Name/Phone/City/Specializations/Aadhaar/Payment/SubmittedAt columns, and filters to PENDING so it never displays even a current rejection reason, let alone history. That profile-level KYC path IS the anti-pattern described: PanditProfile.rejectionReason (schema.prisma:216) is a single column overwritten on every reject (admin.routes.ts:139-145). AdminLog (schema.prisma:651-659) is written on KYC approve/reject (kyc.service.ts:178,196) but has no read endpoint and no UI reader, so it does not deliver the requirement either. Verdict is partial rather than missing strictly because the durable history table plus a listing endpoint returning all versions do exist — the persistence contract this requirement depends on is fully built and guarded. Verdict is not built because the requirement is phrased as what the ADMIN SEES, and an admin today sees zero rejection history through any screen.

**What is missing.** Everything on the admin presentation side. Specifically: (1) no admin route/page consumes GET /admin/pooja-verifications — apps/admin has zero references to it, so there is no pooja-verification review screen at all, not even a current-queue view; (2) consequently no rejection COUNT per (pandit, poojaType) is displayed anywhere; (3) no list of PRIOR rejection reasons across versions is displayed; (4) no indicator of WHICH VERSION was finally accepted is displayed; (5) the API endpoint itself (poojaVerification.controller.ts:127-135) returns a flat createdAt-desc list with no grouping by (panditProfileId, poojaType) and no derived attempt-count or accepted-version field, so a UI would need either server-side aggregation added or client-side grouping written; (6) separately, the profile-level KYC rejection path retains NO history at all (PanditProfile.rejectionReason overwritten at admin.routes.ts:143), and AdminLog is write-only with no read endpoint — if F09-04 is intended to cover pandit-level KYC rejections too, that half needs a history source before any UI is possible.

**Why the first verdict was downgraded.** All cited code is accurate — I verified each citation — but the grade is inflated. F09-04 (docs/pandit-pov-conformance-register.md:108) reads "Admin sees rejection history: count, reasons, accepted version", typed AUTO (admin). Zero of the three named deliverables exist on any admin surface. Repo-wide grep for "pooja-verifications" and "poojaVerification" across source (dist excluded) hits only services/api/src/routes/admin.routes.ts:49-51, services/api/src/app.ts:258, and apps/pandit/src/app/(dashboard-group)/my-poojas/page.tsx:81 — zero hits in apps/admin. There is no admin route (AdminNav.tsx:5-11 and AdminSidebar.tsx:33-37 list only /verifications) that could reach the endpoint, so it is unreachable dead capacity from the admin app. apps/admin/src/app/verifications/page.tsx:50 is a different feature (fetches /admin/pandits?status=PENDING) with columns at :155-162 containing no version, count, or reason. The investigator's "partial" rests entirely on the persistence layer (schema.prisma:351-373, controller:66-81), but that is the subject of a SEPARATE register line, F09-03 at :107, already marked "✅ guarded" for exactly the version+1 INSERT behaviour. Crediting the same storage work again under F09-04 double-counts one implementation across two requirements and turns a wholly unbuilt admin feature into a "partial". A backing table plus an unconsumed endpoint is a precondition, not a fraction, of an admin-facing requirement. Separately, listPoojaVerifications (controller:127-135) supplies none of the three required shapes even to a hypothetical consumer: flat orderBy createdAt desc, no per-(pandit,poojaType) grouping, no rejection count, no accepted-version marker. Correct verdict is missing; it would become partial once a real admin surface renders at least one of count/reasons/accepted-version.

**Evidence.**
- packages/db/prisma/schema.prisma:348-350 — doc comment on PoojaVerification: 'One row PER SUBMISSION — history is NEVER overwritten. A re-submit inserts a new row with version+1 and status PENDING; a rejection stays in history with its reason.'
- packages/db/prisma/schema.prisma:351-373 — PoojaVerification model carries per-row status, rejectionReason (@db.Text), version Int @default(1), reviewedById, reviewedAt, createdAt; @@index([panditProfileId, poojaType, version]). This IS a history table, not a single status column.
- services/api/src/controllers/poojaVerification.controller.ts:66-81 — submit uses prisma.poojaVerification.create({... version: (latest?.version ?? 0) + 1 }), i.e. INSERT not UPDATE, so prior rejected rows are retained.
- services/api/src/controllers/poojaVerification.controller.ts:163-166 — reject does an update on the specific row id setting status REJECTED + rejectionReason + reviewedById + reviewedAt; the reason is bound to that version, not to the pandit.
- services/api/src/controllers/poojaVerification.controller.ts:137-143 — approve stamps APPROVED on the specific row id, so 'which version was finally accepted' is derivable from the data.
- services/api/src/routes/admin.routes.ts:49 — fastify.get('/pooja-verifications', listPoojaVerifications) exists.
- services/api/src/controllers/poojaVerification.controller.ts:127-135 — listPoojaVerifications with no status query returns ALL rows (full objects, so version/status/rejectionReason/reviewedAt included) ordered by createdAt desc, but is a flat list with no per-(pandit,poojaType) grouping, no rejection count, and no 'accepted version' marker.
- Repo-wide Grep for 'pooja-verifications' (excluding dist) returns only services/api/src/routes/admin.routes.ts:49-51, services/api/src/app.ts:258, and apps/pandit/src/app/(dashboard-group)/my-poojas/page.tsx:81 — ZERO hits anywhere in apps/admin. No admin screen consumes the endpoint.

**Test that would pin it.** Two AUTO guards. (A) Persistence guard (would pass today, locks in the good half): integration test against the API — submit a verification for (pandit, poojaType), admin-reject with reason R1, re-submit, admin-reject with R2, re-submit, admin-approve. Then assert prisma.poojaVerification.findMany({ where: { panditProfileId, poojaType } }) returns exactly 3 rows with versions [1,2,3], rejectionReason values [R1, R2, null], and statuses [REJECTED, REJECTED, APPROVED] — proving nothing was overwritten. (B) Admin-surface guard (fails today, is the actual F09-04 assertion): a source-level guard in the style of services/api/src/lib/verification-resubmit.test.ts asserting that some file under apps/admin/src fetches '/admin/pooja-verifications', plus a component/E2E test that renders the admin pooja-verification review screen against the 3-row fixture from (A) and asserts the DOM contains the rejection count (2), both reason strings R1 and R2, and a marker identifying version 3 as the accepted one. Guard (B) is the one that flips this item to built.

## F32-05 — ❌ missing

**What the code does today.** Connectivity loss does NOT flip the pandit's availability toggle. The app has two entirely separate 'online' concepts: (a) browser connectivity, detected correctly by window 'online'/'offline' listeners in useNetwork.ts and duplicated in OfflineBanner.tsx, whose only effect is rendering a banner; and (b) the pandit availability flag (Prisma PanditProfile.isOnline), held as local state in the home container, seeded once from /auth/me and mutated only by the manual toggle handler or its two voice aliases. No code path connects (a) to (b) — no listener calls setIsOnline, and no listener PATCHes /pandit/status. Additionally, even the visual half of the rule is absent: the toggle has no red state at all; offline renders as softgrey (#E4D6C7 dot), so 'flip to offline (red)' could not be satisfied by the current design tokens.

**What is missing.** Everything: (1) no window 'offline' (or fetch-failure) handler that calls setIsOnline(false) or PATCHes /pandit/status; (2) the GoOnline pill has no red/error visual state — only leaf-green (online), softgrey (offline) and slate-300 (disabled); (3) no reconciliation on reconnect (window 'online' does not restore or re-assert availability to the server).

**Evidence.**
- apps/pandit/src/app/(dashboard-group)/home/page.tsx:39 — the availability flag is plain local React state: `const [isOnline, setIsOnline] = useState(false)`
- apps/pandit/src/app/(dashboard-group)/home/page.tsx:90 — its only seed is the server profile: `setIsOnline(user.panditProfile?.isOnline || false)`
- apps/pandit/src/app/(dashboard-group)/home/page.tsx:215-261 — handleToggleStatus is the ONLY other writer (manual tap / voice command); no listener, no network input
- apps/pandit/src/app/(dashboard-group)/home/page.tsx:309-329 — voice commands go-online/toggle-offline call the same manual handler
- apps/pandit/src/hooks/useNetwork.ts:10-22 — connectivity listeners exist (window 'online'/'offline' → uiStore.setOnline) but write a DIFFERENT store field
- apps/pandit/src/stores/uiStore.ts:56-62 — setOnline only sets uiStore.isOnline + showNetworkBanner; it never touches pandit availability or calls /pandit/status
- apps/pandit/src/components/ui/OfflineBanner.tsx:7-32 — a second, independent connectivity hook (useOnline) that only renders a banner
- apps/pandit/src/components/overlays/NetworkBanner.tsx:20-46 — consumes uiStore.isOnline for a banner only

**Test that would pin it.** Component/integration test on the home route: render HomePage with a mocked /auth/me returning panditProfile.isOnline=true, assert the pill is in the online style; then dispatch `window.dispatchEvent(new Event('offline'))` (with navigator.onLine stubbed false) and assert (a) the pill immediately renders the offline/red state and (b) a PATCH to /pandit/status with {isOnline:false} was issued. Both assertions fail today.

## F32-06 — ❌ missing

**What the code does today.** There is no backgrounding rule of any kind on the availability toggle. Four visibilitychange handlers exist in apps/pandit/src (voiceController, registrationStore, useWakeLock, webotp) and none of them reads or writes the availability flag or calls /pandit/status. The nearest thing to a duration threshold is voiceController.hiddenSince, which is a 2-second speech-resume debounce, and shishyaAgent's IDLE_RESET_MS (10 min) which resets agent chat history only — a coincidental match on the number, not an implementation of this rule. Server-side there is no staleness mechanism either: PanditProfile has no last-seen timestamp column, neither status endpoint records one, and the only scheduled job is the review reminder. Note the second half of the requirement ('less than 10 minutes leaves status unchanged') is vacuously satisfied only because nothing ever changes the status on backgrounding.

**What is missing.** (1) No visibilitychange/pagehide handler that records a hidden-since timestamp for availability; (2) no 10-minute threshold check on return-to-foreground that flips to offline; (3) no server-side fallback — PanditProfile lacks a lastSeenAt/heartbeat column (packages/db/prisma/schema.prisma:211), no endpoint stamps one, and no cron sweeps stale online pandits, so a pandit who backgrounds the app and never returns stays online in the DB indefinitely.

**Evidence.**
- apps/pandit/src/app/(dashboard-group)/home/page.tsx:39,90,215-261 — the availability state has no visibilitychange subscriber and no timer; the only useEffect timer is the 30s booking poll at page.tsx:161-197
- apps/pandit/src/lib/voiceController.ts:1155-1184 — the app's main visibilitychange handler stops speech, aborts listening, releases mic + wake lock on hide and re-arms on show; it never reads or writes availability
- apps/pandit/src/lib/voiceController.ts:156,1158,1179-1184 — `hiddenSince` exists but is used for a 2000 ms audible resume-cue debounce (`performance.now() - this.hiddenSince > 2000`), not a 10-minute rule
- apps/pandit/src/stores/registrationStore.ts:226-239 — the other visibilitychange handler only re-syncs registration form data from localStorage
- apps/pandit/src/lib/hooks/useWakeLock.ts:43-47 and apps/pandit/src/lib/webotp.ts:215-221 — remaining visibilitychange handlers, both unrelated to availability
- apps/pandit/src/lib/shishyaAgent.ts:60 — the ONLY 10-minute constant in the app (`IDLE_RESET_MS = 10 * 60 * 1000`) governs agent chat-history reset, not online status
- packages/db/prisma/schema.prisma:211 — `isOnline Boolean @default(false)`; the model carries no lastSeenAt/lastHeartbeatAt/onlineSince column, so server-side staleness is not even representable
- services/api/src/routes/pandit.routes.ts:170-188 — PATCH online-status is a bare write of the supplied boolean; no timestamp recorded

**Test that would pin it.** Fake-timers component test on the home route: render HomePage with isOnline=true; set document.visibilityState='hidden' and dispatch 'visibilitychange'; advance timers 9 minutes, restore visibility, assert the pill is still online AND no PATCH /pandit/status fired (this passes today, vacuously). Repeat with 11 minutes and assert the pill is offline AND a PATCH {isOnline:false} fired — this fails today. A server-side complement: assert PanditProfile exposes a last-seen timestamp that PATCH /pandit/status updates, and that a sweep marks profiles stale beyond 10 minutes offline.

## F43-01 — ❌ missing  ⬇ *downgraded from partial by the refute pass*

**What the code does today.** There is no universal pooja/samagri ID in the system. What exists is a partial island: eight hardcoded pooja IDs (SATYANARAYAN, GRIHA_PRAVESH, VIVAH, MUNDAN, NAAMKARAN, HAVAN, RUDRABHISHEK, SHRADH) declared as a component-local literal in the readiness page, whose display names ARE resolved per active language through t("onboarding.specializations.<ID>") over the Hindi source bundle plus runtime-translated bundles. For those eight, the requirement genuinely holds. Everything else fails it: the DB `Ritual` model has a cuid id but no stable code/slug and only two hardcoded name columns (name/nameHindi), and no other model FK-references it — every consumer (PujaService.pujaType, DakshinaRate.pujaType, PoojaConfig.poojaType, PoojaVerification.poojaType, SamagriPackage.pujaType, PanditProfile.specializations[], Booking.eventType) stores a bare String. Worse, the two keyspaces collide in one column: readiness writes "SATYANARAYAN" into DakshinaRate/PoojaConfig.pujaType while the my-poojas/add wizard writes the pandit's dictated display name ("सत्यनारायण कथा") into the same unique-constrained column, so one pooja can exist as two rows. Samagri has no entity at all — items are untyped JSON `{itemName, quantity}` / client-side `{name, qty, brand}`, with zero id and zero per-language name. The i18n foundation therefore works only for the eight canned readiness poojas and cannot work for custom poojas or any samagri item.

**What is missing.** (a) No shared/persisted pooja ID: SPEC_LIST is a private literal in readiness/page.tsx, not a DB table, enum, or shared package — my-poojas, bookings, samagri and the customer app do not consume it. (b) Ritual has no stable code/slug and no back-relations; nothing keys off Ritual.id. (c) Custom poojas (the my-poojas/add path) have no ID at all — the dictated display name IS the key, and it lands in the same column as the IDs. (d) Samagri items have NO id and NO per-language name anywhere — schema, API, or client. (e) Ritual's per-language names are two fixed columns (en/hi), not a per-language relation, so a third language has nowhere to live. (f) apps/web drops the real ritual.id and persists the English display name as Booking.eventType.

**Why the first verdict was downgraded.** All 15 of the investigator's citations verified accurate, but their positive half ("for those eight, the requirement genuinely holds") is refuted, so "partial" overstates it. (1) The ID->per-language-name resolution works on only TWO authoring screens: apps/pandit/src/app/(dashboard-group)/readiness/page.tsx:111-112 (used :826,853,868-869,935) and apps/pandit/src/app/(dashboard-group)/samagri/page.tsx:82. Every CONSUMER screen renders the stored string RAW with no t(): my-poojas/page.tsx:228 `<span>{pooja}</span>` where pooja comes from prof?.specializations (:68) — so a pandit who onboarded via readiness (which writes IDs, :397/:405) is shown the literal ASCII string "SATYANARAYAN" on the primary मेरी पूजाएँ hub, and again in the delete aria-label at :233 which the screen reader speaks. Also home/HomeView.tsx:323,392,423; bookings/page.tsx:245; bookings/[id]/page.tsx:194; bookings/[id]/request/page.tsx:172; earnings/page.tsx:175,207 all render `pujaType || eventType` raw. The ID is written to the DB and never resolved again anywhere it is read back — this is a translated chip label, not an identity system. (2) NEW evidence the investigator missed: my-poojas/page.tsx:27 `const ALL_POOJAS = Object.values(hi.onboarding.specializations)` reads the Hindi VALUES directly off the `hi` object, bypassing t() entirely — that screen's picker is hard-pinned to Hindi regardless of active language AND stores display strings, making it the collision generator. (3) An entire named half of the requirement (samagri) has zero implementation: schema.prisma:300-320 SamagriPackage.items is untyped Json, my-poojas/add/page.tsx:174-179 creates {name,qty,brand} with no id — no entity, no id, no per-language name anywhere. Schema claims independently confirmed: grep "Ritual" over the entire schema returns exactly one line (661, the declaration) — nothing FK-references it; free-string pooja identity confirmed at schema.prisma:280, :295, :384, :355, :173, :411. The i18n runtime itself is real (i18n.ts:1-18,91-98 fetches bundles from POST /voice/translate with hi fallback), but it is not wired to the pooja identity anywhere outside the two authoring screens. Correct verdict is missing: there is no universal pooja/samagri ID; what exists is a component-local 8-item hardcoded picker with localized chip labels on two screens.

**Evidence.**
- packages/db/prisma/schema.prisma:661-675 — `model Ritual` has only `id String @id @default(cuid())`, `name`, `nameHindi`, `description`, `descriptionHindi`. No `code`/`slug`/`universalId` field, no `@unique` on name. Per-language names are TWO HARDCODED COLUMNS (en + hi), not a relation and not extensible to the other languages the app offers.
- packages/db/prisma/schema.prisma:661-675 + grep of the whole schema — NOTHING references Ritual: no model has a `ritualId` FK or a `@relation` to it. Ritual is a standalone catalogue island.
- packages/db/prisma/schema.prisma:276-289 `model PujaService { pujaType String // "Vivah", "Griha Pravesh" etc. }` — the pooja identity is a free string column, indexed but unconstrained.
- packages/db/prisma/schema.prisma:291-298 `model DakshinaRate { pujaType String // e.g. "SATYANARAYAN" ; @@unique([panditId, pujaType]) }` — uniqueness is on the STRING, so two spellings of one pooja are two rows.
- packages/db/prisma/schema.prisma:379-391 `model PoojaConfig { poojaType String ; @@unique([panditProfileId, poojaType]) }` and 351-374 `model PoojaVerification { poojaType String; poojaName String? }` — same free-string key.
- packages/db/prisma/schema.prisma:173 `specializations String[] // ["Vivah", "Griha Pravesh", ...]` on PanditProfile — an array of display strings, not IDs.
- packages/db/prisma/schema.prisma:403-418 `model Booking { eventType String // "Vivah", "Griha Pravesh" etc. }` — the booking's pooja identity is also a display string.
- packages/db/prisma/schema.prisma:300-320 `model SamagriPackage { pujaType String; items Json }` with the documented shape `[{"itemName":"Desi Ghee","quantity":"500g","qualityNotes":"…"}]` — samagri items are untyped JSON blobs keyed by a human-typed itemName. There is NO samagri item entity, NO id, NO per-language name anywhere in the schema.

**Test that would pin it.** Two AUTO tests. (1) Schema/contract test over packages/db/prisma/schema.prisma (or a generated Prisma client type assertion): assert `Ritual` exposes a non-null unique `code`/`slug`, and assert that PujaService/DakshinaRate/PoojaConfig/PoojaVerification/SamagriPackage/Booking reference that key rather than declaring a bare `String` pujaType/eventType — today this fails on every model. (2) Collision test at the client boundary: drive readiness StepR1 to select SATYANARAYAN and capture the POST body (expect pujaType==="SATYANARAYAN"), then drive my-poojas/add with name="सत्यनारायण कथा" and capture its POST body; assert both resolve to the SAME key. Today they differ, which pins the two-keyspaces bug. Plus a samagri test asserting each item in the SamagriPackage payload carries a stable `itemId` — currently no such field exists.
