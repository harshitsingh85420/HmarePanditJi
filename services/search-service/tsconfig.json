{
  "// note": "search-service is plain JavaScript (see README TODO to convert it). allowJs + checkJs:false lets `tsc --noEmit` succeed here instead of falling through to the root tsconfig and compiling the whole repo.",
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "allowJs": true,
    "checkJs": false,
    "noEmit": true,
    "skipLibCheck": true
  },
  "include": ["src/**/*"]
}
